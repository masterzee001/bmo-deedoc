# Sprint 4 Final Audit — All 140 Features

- **Baseline:** `main @ c41d23ad87189331680723d4a30ef10a22d30717`
- **Audit date:** 2026-08-10
- **Method:** fresh evidence-based re-audit of the merged repository. Every
  `COMPLETE` claim was independently challenged by a second reviewer before
  being accepted.
- **Status of this document:** this is the **current** implementation audit.
  Earlier sprint reports remain in `docs/` as history and must not be cited as
  current state.

> The Sprint 3 counts (74 COMPLETE / 62 PARTIAL / 4 BLOCKED) were a historical
> starting point only. They were **not** carried forward. Every status here was
> re-derived from the code on this baseline, and most Sprint 3 `COMPLETE` labels
> did not survive that re-derivation.

## Result

| Status | Count |
|---|---:|
| COMPLETE | 13 |
| PARTIAL | 123 |
| BLOCKED | 4 |
| **Total** | **140** |

**Readiness:** VPS staging is approved for **infrastructure validation only**;
product/UAT staging is **not**. Six of the defects below need no VPS at all, so
provisioning a host does not advance them. The P0 closure backlog and the
two-track sequencing are in
[`docs/VPS_STAGING_READINESS.md`](VPS_STAGING_READINESS.md).

## Why this differs so sharply from Sprint 3

The Sprint 3 matrix largely asked *"does an implementation exist?"*. This audit
asks the locked question: *"does the intended workflow run end-to-end, for the
intended role, with authorization, persistence and evidence?"*

Five systemic findings account for most of the movement. Each is a concrete,
path-cited defect, not a difference of opinion:

1. **The legacy national surface is still mounted.** `apps/api/src/routes/admin.ts`
   (~6,000 lines) still operates on `GeoPoliticalZone` and `AdminLevel`
   `NATIONAL`/`GEO_POLITICAL_ZONE`/`STATE`, `GET /auth/territories/states`
   returns all 37 states, and `POST /auth/register-voter` accepts any `stateId`.
   Ogun-only is a property of the new command layer, not of the platform.
   Affects 001, 003.

2. **LGA is still a live command level in the legacy layer.** `apps/api/src/scope.ts`
   ranks `LGA:1` and `POST /admin/users` creates `AdminProfile` records with
   `adminLevel: 'LGA'`. The target layer correctly excludes it; the legacy layer
   does not. Affects 003.

3. **Specialist roles could not reach their own workspace.** `VALIDATOR` and
   `PAYOUT_OFFICER` were rejected by the only page that issues an admin session,
   so the verification queue and payout queue were dead UI for the only roles the
   API permits to use them. **Fixed in this PR** (see below). Affected 008, 027,
   028, 030, 056.

4. **Member constituency ancestry is never populated on the real write path.**
   `VoterProfile.senatorialDistrictId` / `federalConstituencyId` /
   `stateConstituencyId` are nullable, the public registration form collects only
   State → LGA → Ward → PU, and `apps/api/src/routes/auth.ts` stores
   `parsed.data.X || null` without deriving ancestry from the ward it already
   validated. Every member registered through the product is therefore invisible
   at three of the six dashboard levels, which band `CRITICAL` regardless of real
   strength. Affects 059–065, 077.

5. **Two disjoint reward ledgers with an ungated money-out path.**
   `RewardLedgerEntry` feeds the threshold-gated payout module, while the legacy
   `RewardLedger` feeds `POST /voter/redemptions`, which checks only
   `pointsRequested > availablePoints` and accepts a **client-supplied**
   `amountRequested` — never consulting `PayoutConfiguration.minimumPoints` or
   `pointConversionRate`. Affects 049, 051, 058.

## Corrections to previously reported status

Two claims made in earlier sprint reports did not survive this audit and are
corrected here:

- **Feature 110 (In-App Voice Calling)** was reported COMPLETE in PR #6. That was
  wrong. `apps/web/components/voice-call-panel.tsx` creates an
  `RTCPeerConnection`, adds local tracks and sets `ontrack`, but never calls
  `createOffer`, `createAnswer`, `setLocalDescription`, `setRemoteDescription`,
  or `addIceCandidate`, and never invokes the `sendElectionDayCallSignal` client
  that exists in `apps/web/lib/api.ts`. **No SDP or ICE is ever exchanged, so a
  call can never carry audio** — the UI displays "Connected" from the durable
  REST state while no media path exists. The durable lifecycle, permissions and
  history are real and tested; the media negotiation is absent. **PARTIAL.**

- **Feature 139 (Evidence Retention Policy)** is **BLOCKED**, not PARTIAL. The
  repository contains only the hard-coded string
  `ELECTION_EVIDENCE_STRONG_RETENTION_TARGET` and a `LEGAL_HOLD` enum value.
  Retention duration, legal-hold duration and destruction policy are governance
  decisions and must not be invented.

## Directed re-reviews

| Feature | Status | Reasoning |
|---|---|---|
| **096** Live Election Operations Map | PARTIAL | `buildLiveMapMarkers` plots agents and incidents only; the `pollingUnits` array is never turned into markers. A validated PU map additionally needs geodata that does not exist. |
| **100** PU Geofence Monitoring | BLOCKED | Requires approved PU latitude/longitude/radius. Code correctly returns `GATED_AUTHORITATIVE_PU_GEODATA_REQUIRED` rather than guessing. |
| **102** Location Mismatch Alerts | BLOCKED | Same geodata dependency; the comparison has no expected location to compare against. |
| **110** In-App Voice Calling | PARTIAL | No SDP/ICE negotiation in the client (above). This is a **code gap**, independent of the unproven TURN relay path. |
| **111** Call Permissions, Interface and History | PARTIAL | Permissions, lifecycle and history are implemented and tested, but the callee is never notified: the panel refreshes only on mount and after its own actions, with no realtime subscription to `call.ringing`. |
| **127** Preview and Streaming Derivatives | PARTIAL | Image thumbnail/preview generation, original SHA-256 verification before derivation, separate derivative namespace, retry lifecycle and durable scheduling all exist and are tested. **No video transcoding runtime exists** (video records an explicit `DEFERRED`), and no endpoint signs a derivative key, so derivatives cannot yet be read. Not upgraded on the strength of the image path alone. |
| **139** Evidence Retention Policy | BLOCKED | Governance decision outstanding (above). |

## Political safety boundary — verified clean

Features 034–058 were checked against the locked boundary. The only reward
triggers are `VOTER_VERIFICATION_APPROVED` and `REFERRAL_MILESTONE_REACHED`
(`packages/shared/src/platform-contracts.ts`), and the ledger categories are
`VERIFIED_REFERRAL`, `FIELD_ACTIVITY`, `TASK_COMPLETION`,
`APPROVED_PARTICIPATION`, `BONUS`, `MANUAL_ADJUSTMENT`.

**No implementation path rewards ballot choice, candidate vote choice, or proof
of voting for a candidate.** The boundary holds.

## Authoritative Ogun data

Unchanged and still the dominant external constraint:

| Level | Status |
|---|---|
| Ogun State | VERIFIED |
| Senatorial Districts (3) | VERIFIED |
| Federal Constituencies (9) | VERIFIED |
| State Constituencies (26) | PARTIAL — 26 valid source rows, **0 loaded** (the model requires an `lgaId` that does not exist) |
| LGAs (20) | MISSING |
| Wards | MISSING |
| Polling Units | MISSING |
| PU geodata | MISSING |

Because `resolveOperationalTerritory` fails closed on incomplete ancestry and
`organization-tree.ts` returns 409 on the Ogun counts, **no State Constituency,
Ward or Polling Unit coordinator can be created against real data.** Every
command-hierarchy test below Federal Constituency passes only against synthetic
fixtures.

This is correct fail-closed behaviour and must not be relaxed. Missing
authoritative data is never rendered as legitimate zero-strength territory —
the dashboard reports `referenceDataIncomplete` instead.

## Full matrix

| ID | Name | Status | Implementation evidence | Test evidence | Remaining gap | Blocker |
|---|---|---|---|---|---|---|
| 001 | Ogun-State-Only Platform | PARTIAL | Ogun-only enforcement exists in the new command layer: packages/shared/src/platform-contracts.ts defines OGUN_STATE_ID='ng-state-ogun'; apps/api/src/authorization.ts resolveOperationalTerritory throws TerritoryAuthorizationError('OUTSIDE_OGUN') for any other state and re-validates every SD/FC/SC/Wa… | apps/api/src/phase1-architecture.test.ts case 'RBAC matrix enforces own, subordinate, peer, superior, specialist, and LGA boundaries' and 'organization tree rejects incomplete ref… | The nationwide PICS surface is still live and unrestricted. apps/api/src/routes/auth.ts calls ensureNationalReferenceStates() and GET /auth/territories/states returns all 37 states; POST /auth/register-voter accepts any stateId (only internal parent consistency is checked in apps/api/src/lib/territ… | — |
| 002 | Constituency-First Command Structure | PARTIAL | apps/api/src/authorization.ts resolveOperationalTerritory() walks PU -> Ward -> State Constituency -> Federal Constituency -> Senatorial District, claiming each parent and rejecting INCOMPLETE_COMMAND_HIERARCHY when a Ward has no stateConstituencyId or an SC has no federalConstituencyId; validateCo… | apps/api/src/pre-election.test.ts walks STATE -> SENATORIAL_DISTRICT -> FEDERAL_CONSTITUENCY -> STATE_CONSTITUENCY -> WARD -> POLLING_UNIT asserting each level names its own child… | None for the command-structure engine itself. (Enforcement is application-layer only; no DB CHECK constraints exist in packages/database/prisma/ogun-migrations.) [challenged: The code chain is real, but three of the five command levels have no authoritative Ogun data and the loader cannot produce t… | — |
| 003 | LGA as Reference, Not Command Level | PARTIAL | CoordinatorLevel in packages/database/prisma/schema.prisma has no LGA member; apps/api/src/authorization.ts never reads lgaId; packages/database/scripts/migrate-phase1-identities.ts blocks LGA admins with reason 'LGA is reference-only and grants no command role.'; LGA remains a reference relation o… | apps/api/src/phase1-architecture.test.ts asserts a ward coordinator gets no access to a peer branch that shares the same LGA ('Both branches share the same LGA; that association g… | LGA is still a live command level in the legacy layer that is mounted in production code. apps/api/src/scope.ts levelRank includes LGA:1 and matchesAdminScope()/canCreateAgentInScope() branch explicitly on adminProfile.lgaId; POST /admin/users (apps/api/src/routes/admin.ts:1898) creates AdminProfil… | — |
| 004 | Ogun Electoral Reference Database | BLOCKED | All seven levels are modelled in packages/database/prisma/schema.prisma (State, SenatorialDistrict, FederalConstituency, StateConstituency, LGA, Ward, PollingUnit) with provenance columns (sourceCode, sourceCodeNamespace, referenceImportReleaseId) and a ReferenceDataImportRelease model; an approved… | No API integration test covers the reference importer; verification is via CLI scripts (npm run verify:reference:ogun, verify:reference:ogun-contract). apps/api/src/phase1-archite… | docs/OGUN_REFERENCE_DATA_READINESS.md records State/Senatorial(3)/Federal(9) VERIFIED but State Constituencies PARTIAL (26 valid source rows, 0 loaded because the legacy StateConstituency model requires an lgaId that does not exist) and LGAs, Wards, Polling Units and PU geodata MISSING with no appr… | EXTERNAL_DATA |
| 005 | Locked User-Role Architecture | PARTIAL | packages/shared/src/platform-contracts.ts TARGET_AUTH_ROLES lists the six account roles and COORDINATOR_LEVELS the five coordinator levels; packages/database/prisma/schema.prisma UserRole enum contains SUPER_ADMIN, STATE_OFFICER, COORDINATOR, VALIDATOR, PAYOUT_OFFICER, MEMBER plus explicitly-labell… | apps/api/src/phase1-architecture.test.ts creates fixtures for every target role and asserts VALIDATOR, PAYOUT_OFFICER and MEMBER are denied command actions; apps/api/src/pre-elect… | Four of the ten roles have no provisioning path anywhere in the codebase. Grep across apps/api/src and packages/database/scripts finds no create path for VALIDATOR or PAYOUT_OFFICER (test fixtures write them directly with prisma.user.create); STATE_OFFICER exists only as an output of packages/datab… | — |
| 006 | Super Admin Authority | PARTIAL | apps/api/src/authorization.ts authorizeAction/canManageTargetUser/canAssignTerritory/canCreateCoordinator all short-circuit true for SUPER_ADMIN; SUPER_ADMIN-exclusive routes cover reward rules, payout configuration/cycles/batches/approval and strength metrics/weights (apps/api/src/routes/pre-elect… | apps/api/src/admin-global-structures.test.ts 'non-super-admin cannot create, update, or delete global structures' and 'super admin can deactivate and reactivate managed users'; ap… | Three of the nine listed authority areas are absent. (1) Users/roles: Super Admin cannot create a VALIDATOR, PAYOUT_OFFICER or STATE_OFFICER through any endpoint or UI. (2) Platform settings / configuration / security: grep for PlatformSetting or /settings across apps and packages returns nothing —… | — |
| 007 | General State Officer | PARTIAL | STATE_OFFICER is a first-class role: apps/api/src/authorization.ts grants it Ogun-wide authority for all four Phase-1 actions and lets it manage COORDINATOR/MEMBER targets anywhere in Ogun; apps/api/src/lib/organization-tree.ts surfaces active state officers on the tree root; requireRole('SUPER_ADM… | apps/api/src/phase1-architecture.test.ts logs in as the state officer to load the full unscoped org tree, create a domain candidate, and suspend/restore a ward coordinator; apps/a… | No creation path — a GSO can only appear via packages/database/scripts/migrate-phase1-identities.ts MAP_ADMIN_TO_STATE_OFFICER from a legacy STATE-level ADMIN. Operational visibility is thin: every route in apps/api/src/routes/admin.ts is requireRole('ADMIN','SUPER_ADMIN'), so a STATE_OFFICER recei… | — |
| 008 | State Validator Role | PARTIAL | apps/api/src/routes/pre-election.ts gates the verification queue (GET /verifications, VALIDATOR+SUPER_ADMIN), claim (POST /verifications/:id/claim, VALIDATOR only), decision (PATCH /verifications/:id/decision, VALIDATOR only) and document access (GET .../documents/:id/access, VALIDATOR+SUPER_ADMIN)… | apps/api/src/pre-election.test.ts asserts a validator gets 403 creating a reward rule and 403 on the command dashboard, then successfully claims, accesses a document and approves;… | No way to create a VALIDATOR account (no API route, no UI, no script — apps/api/src/pre-election.test.ts and phase1-architecture.test.ts both insert them with prisma.user.create). The queue is not scoped: buildScopedVerificationWhere returns {} when no territoryType/territoryId is supplied (apps/ap… | — |
| 009 | Payout Officer Role | PARTIAL | PAYOUT_OFFICER role in schema and TARGET_AUTH_ROLES; apps/api/src/routes/pre-election.ts POST /payout/cycles/:cycleId/batches assigns a batch to one payoutOfficerUserId; GET /payout/assignments filters payoutOfficerUserId to the caller; PATCH /payout/assignments/:id/status throws NOT_ASSIGNED (403)… | apps/api/src/pre-election.test.ts asserts a second payout officer gets 403 on another officer's assignment, that PROCESSING then PAID succeed with a PayoutTransaction row, that a … | Delegation is limited only by batch and assigned beneficiary. packages/database/prisma/schema.prisma PayoutAssignment carries no territory columns and no coordinator-level column, and no code path constrains a payout officer by territory or by beneficiary user level, so two of the four limits named… | — |
| 010 | Senatorial District Coordinator | PARTIAL | CoordinatorLevel.SENATORIAL_DISTRICT with rank 5 in apps/api/src/authorization.ts; validateCoordinatorAssignment rejects an SD assignment that also carries FC/SC/Ward/PU ids; canCreateCoordinator/canAssignTerritory allow it to create and reassign strictly lower levels inside its own district; apps/… | apps/api/src/phase1-architecture.test.ts creates a SENATORIAL_DISTRICT coordinator fixture and exercises the rank matrix; apps/api/src/pre-election.test.ts asserts the SENATORIAL_… | Management is API-only. apps/web/lib/api.ts contains no client function for POST /platform/coordinators, PATCH /platform/coordinators/:id/assignment or PATCH /platform/users/:id/status, and no page in apps/web/app exposes them — the only coordinator-facing page, apps/web/app/platform/page.tsx, is r… | — |
| 011 | Federal Constituency Coordinator | PARTIAL | CoordinatorLevel.FEDERAL_CONSTITUENCY rank 4 in apps/api/src/authorization.ts; validateCoordinatorAssignment forbids SC/Ward/PU ids at this level and derives the senatorial parent; isTerritoryDescendant grants access to any resource whose resolved federalConstituencyId matches; scopeOrganizationTre… | apps/api/src/phase1-architecture.test.ts mocks a FEDERAL_CONSTITUENCY actor and asserts authorizeAction(federal,'VIEW_TERRITORY', ownPollingUnitTerritory) === true, proving downwa… | No UI for creating, editing, reassigning, activating or deactivating subordinate coordinators; no ward/PU management or campaign-performance management surface beyond the read-only /admin/command dashboard. Operating this level end to end also requires State Constituency and Ward reference data tha… | — |
| 012 | State Constituency Coordinator | PARTIAL | CoordinatorLevel.STATE_CONSTITUENCY rank 3; validateCoordinatorAssignment requires a resolved stateConstituencyId and forbids ward/PU ids; resolveOperationalTerritory refuses an SC with no federalConstituencyId (INCOMPLETE_COMMAND_HIERARCHY); scopeOrganizationTree exposes the SC's wards and their p… | apps/api/src/phase1-architecture.test.ts creates a STATE_CONSTITUENCY coordinator fixture and its territory-depth validation; apps/api/src/pre-election.test.ts asserts the STATE_C… | No management UI or edit/reassign client at all (same gap as 010/011). Additionally docs/OGUN_REFERENCE_DATA_READINESS.md records 0 of 26 Ogun State Constituencies loaded, and apps/api/src/lib/organization-tree.ts blocks the whole tree when the count is not 26, so this level cannot currently be ope… | — |
| 013 | Ward Coordinator | PARTIAL | CoordinatorLevel.WARD rank 2; POST /platform/coordinators lets a ward coordinator create a POLLING_UNIT coordinator inside its own ward and refuses a peer ward; canManageTargetUser lets a ward coordinator manage MEMBER users inside the ward; PATCH /platform/users/:id/status covers activation/deacti… | apps/api/src/phase1-architecture.test.ts 'controlled assignment blocks self-change, peer IDOR, and specialist command access' asserts ownCreate 201 / peerCreate 403 / selfChange 4… | Of the six listed responsibilities only referrals and performance have a usable surface. Tasks (apps/api/src/routes/admin.ts:4492-4728) and coverage (:5337) are requireRole('ADMIN','SUPER_ADMIN') so a ward coordinator gets 403; member management is limited to account-status changes with no member l… | — |
| 014 | Polling Unit Coordinator | PARTIAL | CoordinatorLevel.POLLING_UNIT rank 1; apps/api/src/middleware/auth.ts requirePollingUnitFieldCapability accepts a COORDINATOR whose coordinatorProfile.level === 'POLLING_UNIT'; field endpoints exist for check-in/check-out, location pings, incidents, my-status and messages (apps/api/src/routes/elect… | apps/api/src/phase1-architecture.test.ts asserts the migrated agent becomes a POLLING_UNIT coordinator with all operational history intact and can still call /agent/activities; ap… | A PU coordinator created through the product's own creation path cannot do field work. requirePollingUnitFieldCapability additionally requires user.agentProfile (apps/api/src/middleware/auth.ts:91), but POST /platform/coordinators (apps/api/src/routes/platform.ts:255) creates only a coordinatorProf… | — |
| 015 | Member / Supporter Account | PARTIAL | Registration at POST /auth/register-voter (apps/api/src/routes/auth.ts:309) with UI at apps/web/app/register/page.tsx; member session capability via requireMemberCapability (apps/api/src/middleware/auth.ts:78, accepts VOTER or MEMBER); apps/web/app/dashboard/page.tsx shows verification status, revi… | apps/api/src/pre-election.test.ts registers members end to end, logs them in and asserts GET /pre-election/verifications/me returns PENDING; apps/api/src/phase1-architecture.test.… | 'Maintain profile information' has no member UI — updateCurrentUserProfile/updateCurrentUserPassword in apps/web/lib/api.ts are wired only to apps/web/app/admin/account/page.tsx; the member dashboard has no profile or password form. New registrations are created as legacy UserRole.VOTER rather than… | — |
| 016 | Candidate as Campaign Record | PARTIAL | packages/database/prisma/schema.prisma model Candidate carries fullName, officeType, politicalPartyId, portraitUrl/portraitAssetId, campaignSlogan, bio, website/facebook/instagram/x URLs, isPublished/isActive and the full Ogun territory chain, with no required user link (legacyUserId is optional); … | apps/api/src/phase1-architecture.test.ts 'candidate target domain is public data and requires no candidate authentication' creates a GOVERNORSHIP domain candidate as the state off… | No 'election' attribute or Election model exists anywhere in the schema, so the record cannot state which election it belongs to. There is no update or deactivate endpoint for the domain record (only create and read). No UI reads or writes it: apps/web/app/candidates/page.tsx calls /candidate/publi… | — |
| 017 | No Separate Candidate Account | PARTIAL | The Candidate domain entity requires no account (legacyUserId String? in schema.prisma) and is created by SUPER_ADMIN/STATE_OFFICER/COORDINATOR via POST /platform/candidates; the public read endpoints require no authentication; packages/database/scripts/migrate-phase1-identities.ts CREATE_CANDIDATE… | apps/api/src/phase1-architecture.test.ts asserts the domain candidate is created by an officer and read publicly with no candidate token. | Candidate login accounts still exist and are still the ones the product actually uses. UserRole.CANDIDATE remains in the enum, apps/api/src/routes/candidate.ts (1642 lines) is mounted in apps/api/src/app.ts, POST /admin/candidates (apps/api/src/routes/admin.ts:1988) still creates a candidate *user … | — |
| 018 | Role + Territory Access Control | COMPLETE | apps/api/src/authorization.ts authorizeAction(actor, action, resource) implements exactly ROLE + COORDINATOR LEVEL + TERRITORY + ACCOUNT STATUS: it returns false unless actor.isActive && actor.accountStatus === 'ACTIVE' && resource.stateId === OGUN_STATE_ID, then branches on role and, for COORDINAT… | apps/api/src/phase1-architecture.test.ts 'RBAC matrix enforces own, subordinate, peer, superior, specialist, and LGA boundaries' asserts 13 role/level/territory/status combination… | None for the target authorization engine. | — |
| 019 | Territory Isolation | PARTIAL | apps/api/src/lib/organization-tree.ts scopeOrganizationTree() prunes the tree to the caller's own branch and blanks stateOfficers; apps/api/src/routes/dashboard.ts authorizes every drill-down through resolveOperationalTerritory + authorizeAction so a sibling cannot be reached; apps/api/src/routes/p… | apps/api/src/phase1-architecture.test.ts asserts peer-ward create 403 and that the scoped tree contains only the caller's ward; apps/api/src/election-day.test.ts 'Situation Room s… | Three concrete leaks/holes. (1) GET /pre-election/verifications applies no scope when territoryType/territoryId are omitted — buildScopedVerificationWhere returns {} (apps/api/src/routes/pre-election.ts:365) — so a VALIDATOR reads every verification row in the database, including members outside Og… | — |
| 020 | Controlled Territory Assignment | PARTIAL | PATCH /platform/coordinators/:userId/assignment (apps/api/src/routes/platform.ts:284) returns 403 when userId === request.authUser.id ('Coordinators cannot change their own territory or level.'); apps/api/src/authorization.ts canManageTargetUser also refuses actor.id === target.id, and canAssignTer… | apps/api/src/phase1-architecture.test.ts asserts a ward coordinator's self-reassignment attempt returns 403 and that canManageTargetUser(ward, selfAsPollingUnit) === false; also a… | The positive half of the feature has no operator surface: apps/web/lib/api.ts contains no client function for PATCH /platform/coordinators/:userId/assignment or PATCH /platform/users/:userId/status, and no page in apps/web/app calls them, so an authorized superior officer cannot actually perform an… | — |
| 021 | Organization Tree | PARTIAL | apps/api/src/lib/organization-tree.ts loadOgunOrganizationTree() builds the full State -> Senatorial District -> Federal Constituency -> State Constituency -> Ward -> Polling Unit -> coordinator structure (only active, ACTIVE-status COORDINATOR users are listed) and throws OrganizationTreeReference… | apps/api/src/phase1-architecture.test.ts 'organization tree rejects incomplete references and returns only coordinator scope' asserts the 409 blocker code WARD_WITHOUT_STATE_CONST… | The UI stops three levels short of the spec. apps/web/app/platform/page.tsx renders Senatorial District cards, Federal Constituency names with coordinator counts, and then only 'stateConstituency.name: N Ward(s)' — there is no Ward, Polling Unit or coordinator navigation, no drill-down, and no link… | — |
| 022 | Coordinator Management | PARTIAL | Create: POST /platform/coordinators (apps/api/src/routes/platform.ts:233) with validateCoordinatorAssignment + canCreateCoordinator + duplicate-email 409 + COORDINATOR_CREATED audit. Assign/reassign: PATCH /platform/coordinators/:userId/assignment with canAssignTerritory. Activate/deactivate: PATCH… | apps/api/src/phase1-architecture.test.ts asserts create-in-own-scope 201, create-in-peer-scope 403, self-reassign 403, suspend 200 with immediate token invalidation, and restore 2… | 'Edit' is unimplemented — there is no endpoint to change a coordinator's name, email, phone or password. There is no coordinator list/detail endpoint for review (only names inside the tree). And the whole capability has no UI: apps/web/lib/api.ts has no coordinator client functions and apps/web/app… | — |
| 023 | Member Management | PARTIAL | Restriction of sensitive evidence is enforced: GET /pre-election/verifications/:id/documents/:docId/access is requireRole('VALIDATOR','SUPER_ADMIN') and writes a VERIFICATION_DOCUMENT_ACCESS_GRANTED audit row (apps/api/src/routes/pre-election.ts:1076); apps/api/src/routes/admin.ts:3226 excludes vot… | apps/api/src/phase1-architecture.test.ts asserts canManageTargetUser(wardCoordinator, member, ownTerritory) === true and that a state officer with no resolved target territory is … | There is no member search/list endpoint for STATE_OFFICER or COORDINATOR — the only member directory is GET /admin/voters, which is requireRole('ADMIN','SUPER_ADMIN'). No member-management UI exists in the coordinator surface (apps/web/app/admin/manage/users/page.tsx is the legacy admin/candidate/a… | — |
| 024 | Member Registration | PARTIAL | POST /auth/register-voter (apps/api/src/routes/auth.ts:309) validates identity (fullName, voterCardNumber min 5), contact (email, digits-only phone), electoral territory (state/lga/ward/pollingUnit with parent-consistency checks), referral (referredByCode resolved against ReferralCode or VoterProfi… | apps/api/src/pre-election.test.ts registerMember() drives the full payload with consent and referral code and asserts 201 plus a PENDING_VERIFICATION Referral row; apps/api/src/ad… | Registration creates role UserRole.VOTER, not MEMBER, so every new account is a legacy identity. The captured 'electoral territory' omits the platform's own command levels — the form has no Senatorial District, Federal Constituency or State Constituency input, and the API leaves those columns null.… | — |
| 025 | Structured Territory Capture | PARTIAL | apps/web/app/register/page.tsx uses cascading <select> elements populated from GET /auth/territories/states|lgas|wards|polling-units — no free-text territory input exists. The server re-validates: apps/api/src/lib/territory.ts validateTerritoryReferences() checks each id exists and belongs to its p… | apps/api/src/pre-election.test.ts passes canonical reference ids created as fixtures and asserts 201; no negative test asserts that an invalid or mismatched territory id is reject… | The controlled record set captured is the legacy LGA-first set only. VoterProfile requires lgaId and wardId but leaves senatorialDistrictId, federalConstituencyId and stateConstituencyId optional, and the registration UI never offers them, so a registration cannot express the locked command hierarc… | — |
| 026 | Voter-Card Upload | PARTIAL | Members can submit evidence at registration (voterDocument block in registerVoterSchema, apps/api/src/routes/auth.ts:66) and afterwards via POST /pre-election/verifications/me/documents (apps/api/src/routes/pre-election.ts:823) or POST /voter/verification/submit; mime type is restricted to jpeg/png… | apps/api/src/pre-election.test.ts submits a document at registration, resubmits a second document (201, documents.length >= 2), and asserts a second member reusing the same sha256… | The file itself is never uploaded. The browser fabricates the storage key client-side — `voter-verification/client/${crypto.randomUUID()}-${safeStorageName(file.name)}` at apps/web/app/register/page.tsx:169 and apps/web/app/dashboard/page.tsx:206 — and posts only metadata; the API stores storagePro… | — |
| 027 | Verification Status Model | PARTIAL | packages/database/prisma/schema.prisma enum VoterVerificationStatus contains exactly NOT_SUBMITTED, PENDING, UNDER_REVIEW, RESUBMISSION_REQUIRED, VERIFIED, REJECTED, plus isFlagged/fraudReason on VoterVerification for the suspicious case. All six are reachable and transitions are enforced: registra… | apps/api/src/pre-election.test.ts walks PENDING -> resubmit(201) -> claim(200) -> APPROVE(200) -> re-approve(409) and asserts the flagged/duplicate case has isFlagged true with fr… | None for the status model itself. [challenged: The enum and transition enforcement are real and the member half works (registration at routes/auth.ts:498-503 sets NOT_SUBMITTED/PENDING; apps/web/app/dashboard/page.tsx:346-355 shows the status pill, review note, flag banner and resubmit form). But f… | — |
| 028 | Validator Work Queue | PARTIAL | GET /pre-election/verifications (apps/api/src/routes/pre-election.ts:923) accepts status (PENDING, UNDER_REVIEW, RESUBMISSION_REQUIRED, VERIFIED, REJECTED), a flagged boolean, free-text search over VIN/name/email/phone, optional territoryType+territoryId (authorized through canViewCommandTerritory)… | apps/api/src/pre-election.test.ts asserts GET /pre-election/verifications?status=PENDING returns the case with >= 2 documents and that the CSV export contains the memberName heade… | The flagged queue is not reachable from the UI — the API's `flagged` parameter is never sent by apps/web/lib/api.ts or the page, so flagged cases can only be spotted inline. There are no queue counts (the page's verificationCounts is computed from the single loaded page), no completed-vs-open separ… | — |
| 029 | Secure Voter-Document Storage | PARTIAL | Public URLs are structurally prohibited: the zod schemas refine originalStorageKey and previewStorageKey with !/^https?:\/\//i ('Document storage key must not be a public URL') in apps/api/src/routes/auth.ts:66-84, pre-election.ts:51 and voter.ts:80. Access requires authentication and authorization… | apps/api/src/pre-election.test.ts asserts the access response returns a string accessToken and that the returned storageKey does not match /^https?:\/\//i. | There is no storage and no real short-lived access. The endpoint returns crypto.randomUUID() as accessToken plus the raw key (apps/api/src/routes/pre-election.ts:1115-1120) — no signed URL, no object, no server-side expiry enforcement, no revocation, and nothing that can actually retrieve bytes. Th… | — |
| 030 | Validation Decision Workflow | PARTIAL | PATCH /pre-election/verifications/:verificationId/decision (apps/api/src/routes/pre-election.ts:1124) is requireRole('VALIDATOR') and accepts APPROVE / REJECT / REQUEST_RESUBMISSION with an optional note, running in one transaction that updates status, reviewedByUserId, reviewedAt and reviewNote, w… | apps/api/src/pre-election.test.ts asserts approve 200 with note, exactly one VERIFIED_REFERRAL ledger entry, referral moved to REWARD_PROCESSED, a repeat approval 409 with still e… | None for the decision workflow. (Its practical value is limited by feature 026/029: the document being decided on is never actually stored or viewable.) [challenged: The workflow is not reachable end-to-end by the intended role. apps/api/src/routes/pre-election.ts:1007 (claim) and :1124 (decision) … | — |
| 031 | Verification History | PARTIAL | packages/database/prisma/schema.prisma:867 `VoterVerificationHistory` stores verificationId, actorUserId (reviewer), fromStatus/toStatus, decision (VoterVerificationDecision incl. SUBMITTED/CLAIMED/APPROVED/REJECTED/RESUBMISSION_REQUIRED/FLAGGED), note, metadataJson, createdAt, with no update/delet… | apps/api/src/pre-election.test.ts:309-331 resubmits evidence and asserts the case still exposes >=2 documents; :345-356 claims then approves through the API so CLAIMED/APPROVED hi… | No reachable UI can produce reviewer history: claim and decision are `requireRole("VALIDATOR")` (apps/api/src/routes/pre-election.ts:1007,1124) but VALIDATOR is not in the allow-list at apps/web/app/admin/login/page.tsx:23 (ADMIN/SUPER_ADMIN/STATE_OFFICER/COORDINATOR only) and is rejected by apps/w… | — |
| 032 | Duplicate / Fraud Screening | PARTIAL | Duplicate voter identifier: `VoterProfile.voterCardNumber` is @unique (packages/database/prisma/schema.prisma:696) and rejected with 409 at apps/api/src/routes/auth.ts:346-355. Repeated evidence use: cross-account SHA-256 lookup flags the case at apps/api/src/routes/auth.ts:487-497 and apps/api/src… | apps/api/src/pre-election.test.ts:559-568 registers a second member reusing the first member's document hash and asserts isFlagged===true and fraudReason==="DUPLICATE_DOCUMENT_HAS… | Only one fraud signal exists (duplicate document hash). No duplicate-verified-account detection beyond unique email/voterCardNumber (phone, name and address are unconstrained). `VoterVerification.voterIdentifier` has an index but no unique constraint (schema.prisma:843). No suspicious-registration-… | — |
| 033 | Consent and Privacy Records | PARTIAL | packages/database/prisma/schema.prisma:698-703 `VoterProfile` persists contactConsent, termsAcceptedAt, privacyAcceptedAt, documentConsentAt and consentVersion; written at apps/api/src/routes/auth.ts:449-453 and refreshed on each evidence resubmission (apps/api/src/routes/pre-election.ts:891-894). … | apps/api/src/election-day.test.ts:239-248 proves GPS consent is enforced fail-closed (location pings rejected with "GPS tracking consent is required"). Voter consent fields are ex… | Consent capture is not fail-closed: acceptTerms/acceptPrivacy/contactConsent/confirmAdult are `z.boolean().optional()` (apps/api/src/routes/auth.ts:60-64) and apps/api/src/routes/auth.ts:316 only rejects an explicit `false`, so omitting them registers the user while auth.ts:450-451 stamps termsAcce… | — |
| 034 | Unique Referral Code and Link | PARTIAL | apps/api/src/auth/referral.ts:7-27 generates a collision-checked `OGUN`+hex code against both `ReferralCode.code` and `VoterProfile.referralCode` (both @unique, packages/database/prisma/schema.prisma:888,697). apps/api/src/routes/pre-election.ts:761-787 (`GET /pre-election/referral-code`) issues/re… | apps/api/src/pre-election.test.ts:287-289 calls /pre-election/referral-code as a coordinator, asserts 200 and a string code; :291-295 then registers a member with that code and :3… | No UI surfaces the code or the shareable link to a coordinator. `fetchPreElectionReferralCode` exists at apps/web/lib/api.ts:366 but is imported by no page (grep across apps/web returns only its definition); apps/web/app/admin/pre-election/page.tsx never renders it and apps/web/app/dashboard/page.t… | — |
| 035 | Referral Attribution | COMPLETE | apps/api/src/routes/auth.ts:403-441 resolves the referrer from either a coordinator ReferralCode or a member's VoterProfile.referralCode, rejects unknown codes (400) and self-referral (auth.ts:431-433); auth.ts:542-554 creates the `Referral` row inside the same transaction as the user, storing refe… | apps/api/src/pre-election.test.ts:291-303 registers with the coordinator's code and asserts the Referral row; :358 asserts the VERIFIED_REFERRAL ledger entry landed on the referre… | None material. Referral rows carry no immutable snapshot of the referrer's coordinator level at registration time, so historical attribution reporting reflects the coordinator's current level. | — |
| 036 | Referral Status Model | PARTIAL | packages/database/prisma/schema.prisma:212-220 defines all six states (REGISTERED, PENDING_VERIFICATION, QUALIFIED, REJECTED, FLAGGED, REWARD_PROCESSED). Transitions: apps/api/src/routes/auth.ts:549 sets PENDING_VERIFICATION or FLAGGED at registration; apps/api/src/lib/pre-election-rewards.ts:31-38… | apps/api/src/pre-election.test.ts:302-303 asserts PENDING_VERIFICATION after registration; :359-360 asserts REWARD_PROCESSED after approval; :559-568 exercises the FLAGGED path vi… | REGISTERED is only a Prisma default (schema.prisma:904) and is never set by any code path, so it is unreachable. FLAGGED is a terminal dead end: no endpoint transitions a referral out of FLAGGED and apps/api/src/lib/pre-election-rewards.ts:26 refuses reward for it forever. QUALIFIED is transient in… | — |
| 037 | Verification-Gated Referral Qualification | COMPLETE | No reward is written at registration — apps/api/src/routes/auth.ts:542-554 creates the Referral in PENDING_VERIFICATION/FLAGGED and touches no ledger. The only caller of `processVerifiedReferralReward` is the validator decision handler, and only on APPROVE: apps/api/src/routes/pre-election.ts:1181-… | apps/api/src/pre-election.test.ts:299-300 asserts zero ledger rows for the referrer immediately after the referred member registers; :324 asserts still zero after evidence resubmi… | The gate itself is fully enforced. The approval action that opens the gate is not reachable through the web UI (validators cannot sign in — apps/web/app/admin/login/page.tsx:23), which is recorded against features 031 and 056. | — |
| 038 | Hierarchical Referral Roll-Up | PARTIAL | apps/api/src/routes/pre-election.ts:348-365 `buildScopedVoterProfileWhere` maps STATE, SENATORIAL_DISTRICT, FEDERAL_CONSTITUENCY, STATE_CONSTITUENCY, WARD and POLLING_UNIT onto the referred member's territory (LGA is correctly absent from the command chain). That scope drives the network referral c… | apps/api/src/pre-election.test.ts:369-374 queries /pre-election/referrals/stats at WARD level and asserts direct counts; :715-727 asserts the WARD leaderboard ranks the coordinato… | Roll-up above Ward does not work for real registrations. The public form collects only state -> LGA -> ward -> polling unit (apps/web/app/register/page.tsx:260-299) and apps/api/src/routes/auth.ts:455-459 stores `senatorialDistrictId/federalConstituencyId/stateConstituencyId` as null when not suppl… | — |
| 039 | Direct vs Network Referral Statistics | PARTIAL | apps/api/src/routes/pre-election.ts:1349-1385 (`GET /pre-election/referrals/stats`) returns exactly the four required figures — directRegistrations, directVerifiedRegistrations, networkRegistrations, networkVerifiedRegistrations — with direct counts keyed on `referrerUserId = actor` and network cou… | apps/api/src/pre-election.test.ts:369-374 asserts directRegistrations===1 and directVerifiedRegistrations===1 at WARD scope. networkRegistrations and networkVerifiedRegistrations … | The feature is explicitly a dashboard requirement and no dashboard renders the distinction. `/pre-election/referrals/stats` has no client wrapper in apps/web/lib/api.ts and is called by no page; apps/web/app/admin/pre-election/page.tsx:402 shows only `directVerifiedRegistrations` per coordinator, a… | — |
| 040 | Configurable Reward Engine | PARTIAL | Points are read from a stored rule version rather than a constant: apps/api/src/lib/pre-election-rewards.ts:42-64 selects the effective `RewardRuleVersion` by qualifying event, active flag, effective window, eligible role and eligible coordinator level, and :93-112 writes `points: ruleVersion.direc… | apps/api/src/pre-election.test.ts:280-285 creates a 25-point rule, :351-358 approves a verification and asserts one VERIFIED_REFERRAL entry, :362-367 asserts confirmedPoints===25 … | Only two qualifying events exist (`RewardQualifyingEvent` = VOTER_VERIFICATION_APPROVED, REFERRAL_MILESTONE_REACHED, packages/database/prisma/schema.prisma:221-227), so the engine covers referral work only. All other qualifying work still awards hard-coded or operator-typed points on the legacy `Re… | — |
| 041 | Super-Admin Reward Configuration | PARTIAL | apps/api/src/routes/pre-election.ts:1229-1286 restricts rule creation to `requireRole("SUPER_ADMIN")` and persists reward value (directPoints), qualifying action (qualifyingEvent), eligible role (eligibleRole), eligible coordinator level, effectiveFrom/effectiveUntil (validated at :1241-1243) and t… | apps/api/src/pre-election.test.ts:266-271 asserts a VALIDATOR gets 403 creating a rule; :574-584 asserts a PAYOUT_OFFICER gets 403 creating a bonus rule; :280-285 and :595-606 ass… | Two of the seven listed controls are unimplemented: `upstreamPointsConfig` and `maximumPoints` exist on RewardRuleVersion (packages/database/prisma/schema.prisma:949-950) but are never written or read anywhere in the codebase, so upstream allocations and limits do not exist. Three more are API-only… | — |
| 042 | Reward Rule Versioning | PARTIAL | packages/database/prisma/schema.prisma:944-962 models `RewardRuleVersion` with a per-rule version number (@@unique([rewardRuleId, version])) and its own effectiveFrom. Historical retention is enforced structurally: `RewardEvent.rewardRuleVersion` and `RewardLedgerEntry.rewardRuleVersion` are `onDel… | apps/api/src/pre-election.test.ts:632 asserts a bonus ledger entry carries a rewardRuleVersionId ("a bonus must reference the rule version that produced it"); :382-386 reads the l… | Version 2 can never exist. `POST /pre-election/reward-rules` always creates `version: 1` (apps/api/src/routes/pre-election.ts:1256-1258) and there is no update endpoint for RewardRule or RewardRuleVersion (grep for rewardRule.update returns nothing), so the central claim of the feature — that a rul… | — |
| 043 | System-Controlled Bonus Points | PARTIAL | apps/api/src/lib/pre-election-rewards.ts:149-275 awards BONUS points only from a system-computed count: it recounts QUALIFIED/REWARD_PROCESSED referrals (:162-167), matches active milestone rule versions whose `milestoneThreshold <= qualifiedReferrals` (:174-194), and writes `points: version.direct… | apps/api/src/pre-election.test.ts:574-584 asserts a PAYOUT_OFFICER is 403 creating a bonus rule; :588-593 asserts a bonus rule with no threshold is 400; :609 asserts no bonus befo… | A Super Admin cannot create a bonus rule through the product: the admin reward-rule form (apps/web/app/admin/pre-election/page.tsx:414-430) and its client signature (apps/web/lib/api.ts:623-626) expose only name/directPoints/eligibleRole/eligibleCoordinatorLevel, omitting qualifyingEvent and milest… | — |
| 044 | Approved Points Categories | PARTIAL | packages/database/prisma/schema.prisma:229-236 defines `RewardLedgerCategory` with all six approved values (VERIFIED_REFERRAL, FIELD_ACTIVITY, TASK_COMPLETION, APPROVED_PARTICIPATION, BONUS, MANUAL_ADJUSTMENT), mirrored in packages/shared/src/platform-contracts.ts:57-63. The category is a required … | apps/api/src/pre-election.test.ts:358 asserts a VERIFIED_REFERRAL entry, :382-386 asserts the ledger endpoint returns category VERIFIED_REFERRAL, and :625-630 asserts a BONUS entr… | Only two of the six categories have a write path: `RewardLedgerCategory.VERIFIED_REFERRAL` (apps/api/src/lib/pre-election-rewards.ts:98) and `BONUS` (:251). Grep across apps/ shows FIELD_ACTIVITY, TASK_COMPLETION and APPROVED_PARTICIPATION appear only as enum/leaderboard strings and never as a ledg… | — |
| 045 | Immutable Points Ledger | COMPLETE | packages/database/prisma/schema.prisma:986-1008 `RewardLedgerEntry` retains every required attribute: recipient (userId), points, category, source event (sourceEventType + sourceEventId), applicable rule (rewardRuleVersionId), timestamp (createdAt), reference/related party (relatedUserId, descripti… | apps/api/src/pre-election.test.ts:358 and :557 assert the ledger row count stays at exactly 1 across a replayed approval; :362-367 asserts the derived balance (confirmedPoints 25)… | Immutability rests on the absence of mutating code paths rather than a database guarantee (no append-only trigger or revoked UPDATE/DELETE grant), and a second, mutable legacy ledger (`RewardLedger`, schema.prisma:807) still exists alongside it. | — |
| 046 | Pending Potential Points | PARTIAL | apps/api/src/routes/pre-election.ts:582-624 `getPotentialReferralPoints` counts referrals still in PENDING_VERIFICATION and multiplies by the effective rule version's directPoints, returning 0 when no rule applies. The balance endpoint (pre-election.ts:1296-1320) returns pendingPotentialPoints as a… | apps/api/src/pre-election.test.ts:299-300 and :324 assert zero ledger rows while the referral is pending (i.e. pending value creates no balance); :362-367 asserts confirmedPoints=… | pendingPotentialPoints is never asserted with a non-zero value in any test, and when several rules could apply the projection uses only the single most recent effective version, so the displayed figure is an estimate. [challenged: No test or direct evidence exercises the feature's actual behaviour,… | — |
| 047 | Idempotent Reward Processing | COMPLETE | Three independent layers. (1) RewardEvent is keyed by @@unique([eventType, sourceType, sourceId]) plus a unique idempotencyKey (packages/database/prisma/schema.prisma:980-981) and is written with `upsert` on that key — apps/api/src/lib/pre-election-rewards.ts:66-87 (`verified-referral:${referral.id… | apps/api/src/pre-election.test.ts:551-557 re-issues APPROVE on an already-approved case, asserts 409 and asserts the VERIFIED_REFERRAL ledger count is still exactly 1; :628 assert… | None identified for the two implemented qualifying events. | — |
| 048 | Reward Integrity Boundary | PARTIAL | The rule engine's qualifying-event taxonomy contains no ballot-linked event: `RewardQualifyingEvent` is limited to VOTER_VERIFICATION_APPROVED and REFERRAL_MILESTONE_REACHED (packages/database/prisma/schema.prisma:221-227) and both derive from verified membership / organizational referral work only… | No test asserts the boundary. The nearest evidence is apps/api/src/pre-election.test.ts:299-300/:358 showing the only reward trigger is voter-registration verification, and :715-7… | The boundary is emergent, not enforced. There is no allowlist or policy check that would reject a ballot-choice-shaped qualifying event, and the legacy reward path still pays for expressing candidate preference: apps/api/src/routes/voter.ts:919-935 awards a fixed 5 points for submitting a `PollResp… | — |
| 049 | Minimum Payout Threshold | PARTIAL | apps/api/src/routes/pre-election.ts:1470-1505 lets only SUPER_ADMIN (`requireRole`) define `minimumPoints`, deactivating the prior configuration in the same transaction and writing a PAYOUT_CONFIGURATION_CREATED audit log. The threshold is copied onto each cycle (pre-election.ts:1561 `minimumThresh… | apps/api/src/pre-election.test.ts:388-393 asserts a PAYOUT_OFFICER is 403 setting the configuration; :395-400 sets minimumPoints 25 as SUPER_ADMIN; :420-422 asserts the eligibilit… | The threshold governs the pre-election payout engine only; the parallel legacy redemption path (apps/api/src/routes/voter.ts:564-604) applies no minimum, checking only that the requested points are within the legacy available balance. [challenged: The Super-Admin threshold governs only one of the p… | — |
| 050 | Payout Schedule | PARTIAL | packages/database/prisma/schema.prisma:1009-1023 `PayoutConfiguration` stores `frequency` and `nextPayoutDate`; :1025-1040 `PayoutCycle` stores opensAt, closesAt and payoutDate with a status. apps/api/src/routes/pre-election.ts:1470-1505 persists the configuration (SUPER_ADMIN only) and :1533-1580 … | apps/api/src/pre-election.test.ts:395-400 creates a WEEKLY configuration; :402-413 creates a cycle with opensAt/closesAt/payoutDate and asserts 201. No test asserts any scheduling… | Nothing consumes the schedule. `frequency` is an unvalidated free-text string (apps/api/src/routes/pre-election.ts:107 `z.string().min(3).max(40)`) — WEEKLY/MONTHLY/custom are not modelled — and grep shows both `frequency` and `nextPayoutDate` are only written and echoed back, never read to drive a… | — |
| 051 | Point-to-Value Conversion | PARTIAL | The rate is centrally configured and versioned: `PayoutConfiguration.pointConversionRate Decimal(18,4)` with createdByUserId (packages/database/prisma/schema.prisma:1012), set only by SUPER_ADMIN at apps/api/src/routes/pre-election.ts:1470-1505, which deactivates prior configurations rather than de… | apps/api/src/pre-election.test.ts:395-400 sets pointConversionRate 2 with minimumPoints 25; :430-434 asserts the created assignment is points 25 / amount "50.00"; :420-422 asserts… | The PAYOUT_CONFIGURATION_CREATED audit metadata records minimumPoints and frequency but omits the conversion rate itself (apps/api/src/routes/pre-election.ts:1492); the rate change is still attributable through the retained configuration row and its createdByUserId. [challenged: The core claim -- '… | — |
| 052 | Payout Lifecycle | PARTIAL | packages/database/prisma/schema.prisma:244-252 defines all seven states (PENDING, ELIGIBLE, APPROVED, PROCESSING, PAID, HELD, REJECTED) shared by PayoutCycle, PayoutBatch and PayoutAssignment. Transitions are server-enforced: batch creation sets ELIGIBLE (apps/api/src/routes/pre-election.ts:1677,16… | apps/api/src/pre-election.test.ts:448-452 approves the batch; :461-466 asserts APPROVED -> PROCESSING succeeds; :474-485 asserts PAID succeeds and creates exactly one PayoutTransa… | HELD is a dead end: nothing can leave it because PROCESSING requires status===APPROVED and PAID requires APPROVED or PROCESSING (apps/api/src/routes/pre-election.ts:1867-1876), so a held payout can never be released and paid. PENDING is never assigned to an assignment (they are created ELIGIBLE at … | — |
| 053 | Payout Batches | COMPLETE | packages/database/prisma/schema.prisma:1042-1053 `PayoutBatch` is bound to a `PayoutCycle` (onDelete: Restrict) with a status and approvedAt; assignments hang off both the batch and the cycle (:1055-1078). apps/api/src/routes/pre-election.ts:1648-1740 (SUPER_ADMIN only) computes the eligible set fo… | apps/api/src/pre-election.test.ts:424-434 creates a batch and asserts one assignment at 25 points / "50.00"; :436-438 asserts the batch is listed; :440-445 asserts a duplicate bat… | Batch creation does not check the cycle's opensAt/closesAt window, and a batch cannot be re-opened or have assignments added after creation. | — |
| 054 | Delegated Payout Assignment | COMPLETE | Only SUPER_ADMIN can delegate: apps/api/src/routes/pre-election.ts:1648 `requireRole("SUPER_ADMIN")`, and the delegate is validated as an existing, active PAYOUT_OFFICER before any assignment is written (:1662-1669, INVALID_PAYOUT_OFFICER/400). Each PayoutAssignment persists payoutOfficerUserId wit… | apps/api/src/pre-election.test.ts:416-418 asserts the payout-officer list is served to SUPER_ADMIN; :424-429 creates the batch naming a specific officer; :454-459 asserts a second… | Delegation scope is per-beneficiary only; there is no territory-bounded officer scope, and the UI always hands every eligible beneficiary to one officer (apps/web/app/admin/pre-election/page.tsx:227 sends `payoutEligibility.map(...)`) even though the API accepts a subset via beneficiaryUserIds. | — |
| 055 | Payout-Officer Restrictions | COMPLETE | Every prohibited action is blocked by an explicit role guard, not by UI omission. Reward formulas: `POST /pre-election/reward-rules` is `requireRole("SUPER_ADMIN")` (apps/api/src/routes/pre-election.ts:1229). Verification decisions: claim and decision are `requireRole("VALIDATOR")` (:1007, :1124). … | apps/api/src/pre-election.test.ts:273-278 asserts a PAYOUT_OFFICER is 403 on a verification decision; :388-393 asserts 403 on payout configuration; :574-584 asserts 403 on creatin… | Not directly tested for the payout-cycle date endpoint or territory endpoints, though both are covered by the same requireRole mechanism proven elsewhere in the suite. | — |
| 056 | Payout-Officer Dashboard | PARTIAL | apps/api/src/routes/pre-election.ts:1816-1837 serves the officer's own assignments (auto-scoped to `payoutOfficerUserId` for the PAYOUT_OFFICER role) with an optional status filter across the full PayoutStatus enum, including beneficiary identity, points, amount and the PayoutTransaction history (s… | apps/api/src/pre-election.test.ts:468-472 asserts `GET /pre-election/payout/assignments?status=PROCESSING` returns exactly the officer's own assignment; :454-459 asserts another o… | A Payout Officer cannot reach the dashboard at all: apps/web/app/admin/login/page.tsx:23 admits only ADMIN/SUPER_ADMIN/STATE_OFFICER/COORDINATOR and apps/web/app/login/page.tsx:24 admits only VOTER/MEMBER, so no login page issues the `picsNigeriaAdminToken` the page requires (apps/web/app/admin/pre… | — |
| 057 | Payout Accountability | PARTIAL | packages/database/prisma/schema.prisma:1080-1097 `PayoutTransaction` preserves payoutAssignmentId (which carries beneficiary and batch and cycle), payoutOfficerUserId, a globally @unique paymentReference, pointsRedeemed, amountPaid Decimal(18,2), status, proofStorageKey, note and createdAt, with `o… | apps/api/src/pre-election.test.ts:474-485 marks the assignment PAID with a payment reference, proof storage key and note, and asserts exactly one PayoutTransaction exists for the … | The UI defeats the accountability it records: apps/web/app/admin/pre-election/page.tsx:488 marks a payout paid with a machine-generated reference (`PO-${assignment.id.slice(0,8)}-${Date.now()}`) and no operator input, and offers no field for the real payment reference, the proof storage key or a no… | — |
| 058 | Financial Integrity Controls | PARTIAL | Duplicate rewards: RewardEvent @@unique([eventType,sourceType,sourceId]) + unique idempotencyKey and RewardLedgerEntry @@unique([userId,sourceEventId,category]) with upsert-no-op writes (packages/database/prisma/schema.prisma:980-981,1007; apps/api/src/lib/pre-election-rewards.ts:67-112). Duplicate… | apps/api/src/pre-election.test.ts:551-557 asserts a replayed approval creates no second reward; :628-642 asserts one bonus per milestone and one event row; :440-445 asserts a dupl… | A parallel legacy money path bypasses all of the above. apps/api/src/routes/admin.ts:3884-3905 lets an ADMIN/SUPER_ADMIN mint 1-100 points per call into the mutable `RewardLedger` (packages/database/prisma/schema.prisma:807) via apps/api/src/lib/participation.ts:41-48, with no reward rule, no rule … | — |
| 059 | Hierarchical Dashboard Model | PARTIAL | apps/api/src/routes/dashboard.ts is a single engine parameterised by level: defaultScopeForActor (L39-54) resolves the deepest territory the actor commands (SUPER_ADMIN/STATE_OFFICER -> STATE, otherwise coordinatorProfile PU/Ward/StateCon/FedCon/SenDist); voterWhereFor/coordinatorWhereFor/pollingUn… | apps/api/src/pre-election.test.ts L644-736 walks STATE -> SENATORIAL_DISTRICT -> FEDERAL_CONSTITUENCY -> STATE_CONSTITUENCY -> WARD -> POLLING_UNIT asserting per-level identity, b… | None material. Aggregation, subordinate scoping, enforced authorization, persistence-backed counts, UI and tests are all present. [challenged: The feature is defined as "Dashboards automatically aggregate information according to the user's authorized level and subordinate territory" (docs/MASTER_F… | — |
| 060 | Polling Unit Dashboard | PARTIAL | apps/api/src/routes/dashboard.ts L457-481 builds pollingUnitDetail for the terminal level: operationalStatus (CHECKED_IN / NOT_CHECKED_IN from AgentActivity), checkedInAt, coordinatorName, incidentCount, reportCount, evidenceCount. Tiles (tilesFor L296-321) add registered members, verified members,… | apps/api/src/pre-election.test.ts L694-707 asserts the POLLING_UNIT view is terminal (childLevel null, children empty), exposes pollingUnitDetail, and returns a full 6-level bread… | Of the ten items the feature enumerates, referrals, points and payouts have no PU-level figure anywhere in the dashboard payload (tilesFor L303-310 defines only six tiles, none referral/points/payout), and tasks are not listed for the Polling Unit (FieldTask is only queried in dashboard.ts inside t… | — |
| 061 | Ward Dashboard | PARTIAL | Same engine: apps/api/src/routes/dashboard.ts with level=WARD aggregates Polling Unit children (childrenOf L162-163), counts coordinators and members via coordinatorWhereFor/voterWhereFor, computes strength + band per child (L440-455), and ranks coordinators via leaderboardFor (L327-401). Presentat… | apps/api/src/pre-election.test.ts L649-691 covers the WARD level (childUnit "Polling Units", band on every child, tiles present); L710-712 asserts a Ward Coordinator's default sco… | Referrals, rewards and tasks are named in the feature but have no Ward-level aggregate: dashboard.ts tilesFor (L303-310) has no referral, reward/points or task tile, and RewardLedgerEntry/Referral are never aggregated per territory in this route. "Activity" is not surfaced at Ward level (AgentActiv… | — |
| 062 | State Constituency Dashboard | PARTIAL | apps/api/src/routes/dashboard.ts level=STATE_CONSTITUENCY: children are Wards (childrenOf L160-161), Polling Units are counted through the nested relation in pollingUnitWhereFor L117-118, membership counts via voterWhereFor L83-84, strength/band per child at L440-455, and drill-down/breadcrumb per … | apps/api/src/pre-election.test.ts L649-691 asserts the STATE_CONSTITUENCY level returns childUnitPlural "Wards", a valid strength band, REGISTERED_MEMBERS tile, and a band for eve… | Referrals and activity, both named in the feature, are absent from the payload (no referral or activity tile in tilesFor L303-310; AgentActivity is not aggregated at this level). Coverage is only the covered-Polling-Units count, and readiness has no separate indicator above the Polling Unit level. | — |
| 063 | Federal Constituency Dashboard | PARTIAL | apps/api/src/routes/dashboard.ts level=FEDERAL_CONSTITUENCY aggregates State Constituency children (childrenOf L154-159), rolls Polling Units up through ward.stateConstituency.federalConstituencyId (L115-116), counts coordinators/members, and produces per-child strength bands and drill-down. Presen… | apps/api/src/pre-election.test.ts L649-691 asserts the FEDERAL_CONSTITUENCY level names "State Constituencies" as its child unit, carries a distinct accent, has a valid band and c… | The feature asks for subordinate Wards and Polling Units, rewards and campaign performance. Only the immediate child level (State Constituencies) is listed - Wards and Polling Units below it appear as counts, not rows. There is no reward aggregate anywhere in dashboard.ts, and "campaign performance… | — |
| 064 | Senatorial District Dashboard | PARTIAL | apps/api/src/routes/dashboard.ts level=SENATORIAL_DISTRICT lists Federal Constituency children (childrenOf L148-153) with per-child strengthScore, band, registered/verified members, coordinators, Polling Units and trend (DashboardChildRow build L440-455), plus district-level tiles, strength/band/tr… | apps/api/src/pre-election.test.ts L649-691 asserts the SENATORIAL_DISTRICT level returns childUnitPlural "Federal Constituencies", a valid strength band, a breadcrumb rooted at ST… | None for the stated scope (compare Federal Constituencies + district-level intelligence). The three Ogun Senatorial Districts and nine Federal Constituencies are the VERIFIED authoritative tier, so this level is not data-blocked. [challenged: The feature is "The Senatorial District dashboard compar… | — |
| 065 | Ogun State Dashboard | PARTIAL | apps/api/src/routes/dashboard.ts defaultScopeForActor L40-42 routes SUPER_ADMIN and STATE_OFFICER to level=STATE / OGUN_STATE_ID; countsFor supplies statewide membership, verification, coordinator and Polling Unit KPIs; strengthFor gives the statewide score/band/trend; children are the three Senato… | apps/api/src/pre-election.test.ts L649-691 covers the STATE level (childUnit "Senatorial Districts", breadcrumb[0].level === "STATE", band and tiles present, children banded). | Referrals and activity, both named in the feature, have no statewide KPI (tilesFor L303-310 has no referral or activity tile). Readiness has no statewide indicator distinct from the covered-Polling-Units count. Coverage feeds the strength score as a hard zero when Polling Unit reference data is not… | — |
| 066 | Candidate Campaign Progress Profile | PARTIAL | A login-independent Candidate domain model exists at packages/database/prisma/schema.prisma L508-533 (legacyUserId optional, own territory columns), and public read paths exist at apps/api/src/routes/platform.ts L140-143 and apps/api/src/routes/candidate.ts L744. TerritoryTarget and TerritoryStreng… | apps/api/src/candidate-public.test.ts and apps/api/src/phase1-architecture.test.ts L632-633 exercise the public/domain candidate split, but no test exercises an officer viewing ca… | There is no officer-facing campaign-progress view. Every campaign operations endpoint in apps/api/src/routes/candidate.ts is gated requireRole("CANDIDATE") - /map-summary (L1411), /agent-activity-summaries (L1362), /incidents (L1335), /voters (L1507), /broadcasts (L1547) - so progress cannot be rea… | — |
| 067 | Configurable Strength Score Engine | PARTIAL | Configuration is real and persisted: StrengthMetricDefinition (unique on metric) and StrengthWeightConfiguration with effectiveFrom/effectiveUntil at packages/database/prisma/schema.prisma L1099-1122; SUPER_ADMIN-only write routes apps/api/src/routes/pre-election.ts L1937-1975 (metrics upsert, effe… | apps/api/src/pre-election.test.ts L501-537 creates a metric, a weight, a target and calculates a snapshot, asserting the returned breakdown contains VERIFIED_MEMBERS with actualVa… | Weighting is still hard-coded on the path that actually drives the command dashboard and heatmap: apps/api/src/routes/dashboard.ts strengthFor L290-293 falls back to score = (verificationRate*0.6 + coverageRate*0.4)*100 whenever no snapshot exists for the territory, and no snapshot exists by defaul… | — |
| 068 | Polling Unit Strength | PARTIAL | apps/api/src/routes/dashboard.ts strengthFor L268-294 produces a 0-100 score for level=POLLING_UNIT, preferring the configured snapshot engine and falling back to a derived composite; strengthBandFor (packages/shared/src/dashboard-contracts.ts L100-102) assigns CRITICAL/WEAK/MODERATE/STRONG. The sn… | apps/api/src/pre-election.test.ts L686-690 asserts every child of the WARD view (i.e. every Polling Unit) carries a valid band and the correct child level; L694-707 covers the PU … | Readiness inputs are thin: for a single Polling Unit the derived score is verification rate plus a coverage term that is 1 or 0 depending on whether any active agent profile is attached (dashboard.ts L254, L291). Check-in state, task completion and incident load are shown in pollingUnitDetail but c… | — |
| 069 | Ward Strength | PARTIAL | Ward-level score via apps/api/src/routes/dashboard.ts strengthFor L268-294 and, when calculated, via the weighted engine at apps/api/src/routes/pre-election.ts L2008-2095 with WARD-scoped metric actuals (buildScopedVoterProfileWhere / buildScopedCoordinatorWhere / countPollingUnitsInScope L717-719)… | apps/api/src/pre-election.test.ts L515-543 sets a WARD target, calculates a WARD snapshot and asserts the breakdown and target progress (actualValue 1); L649-691 asserts the WARD … | Ward strength does not aggregate subordinate Polling Unit readiness - countsFor recomputes raw counts over the Ward's Polling Units rather than rolling up each Polling Unit's score, so a Ward of uniformly critical Polling Units is not driven by their scores. Field activity is not an input to either… | — |
| 070 | State Constituency Strength | PARTIAL | apps/api/src/routes/dashboard.ts computes a STATE_CONSTITUENCY score plus band and lists banded Ward children with their own scores (L440-455). The configured engine supports STATE_CONSTITUENCY scoping (buildScopedVoterProfileWhere and countPollingUnitsInScope L723-725 in apps/api/src/routes/pre-el… | apps/api/src/pre-election.test.ts L649-691 asserts the STATE_CONSTITUENCY dashboard band and that every Ward child carries a band. No test calculates a STATE_CONSTITUENCY snapshot. | The score is not a summary of subordinate performance - it is recomputed from raw counts over the whole subtree, so subordinate Ward and Polling Unit scores do not feed the parent. There is no bulk/cascading snapshot calculation: a parent snapshot can exist while every child is still on the hard-co… | — |
| 071 | Federal Constituency Strength | PARTIAL | Comparison of subordinate State Constituencies is implemented: apps/api/src/routes/dashboard.ts builds a DashboardChildRow per State Constituency with strengthScore, band, membership, coordinators, Polling Units and trend (L440-455); apps/web/app/admin/command/page.tsx renders them as a colour-band… | apps/api/src/pre-election.test.ts L649-691 asserts the FEDERAL_CONSTITUENCY view lists State Constituency children each with a valid band, which is exactly the comparison surface. | Child scores are almost always the derived hard-coded composite rather than the configured engine, because there is no cascading snapshot job - identification of "weak" structures therefore rests on a 0.6/0.4 formula that is not configurable (dashboard.ts L292). No sorting/filtering by band and no … | — |
| 072 | Senatorial District Strength | PARTIAL | apps/api/src/routes/dashboard.ts produces a district score/band/trend and per-Federal-Constituency child rows including registered/verified members, coordinators and Polling Units (L440-455). The configured engine scopes to SENATORIAL_DISTRICT (pollingUnitWhereFor L113-114; countPollingUnitsInScope… | apps/api/src/pre-election.test.ts L649-691 asserts the SENATORIAL_DISTRICT level, its child unit naming and banded children. | Three of the five summarised dimensions are not implemented: membership growth (no time-series comparison of membership; only the current count), field activity (AgentActivity is never aggregated at district level) and campaign readiness (no readiness metric distinct from the covered-Polling-Units … | — |
| 073 | Overall Ogun State Strength | PARTIAL | apps/api/src/routes/dashboard.ts returns a statewide 0-100 strengthScore, band and trend for level=STATE (strengthFor L268-294, strengthBandFor), plus comparisons between the three Senatorial Districts as banded child rows; apps/web/app/admin/command/page.tsx L114-125 renders the score dial, band l… | apps/api/src/pre-election.test.ts L680-681 asserts the STATE strength score is within 0-100 and the band is one of CRITICAL/WEAK/MODERATE/STRONG; L686-690 asserts each Senatorial … | The statewide number is normally the hard-coded derived composite (dashboard.ts L290-293), not the configured engine, since no scheduled snapshot exists. Its coverage component is computed against loaded Polling Units only, so with Ogun Polling Unit reference data MISSING the statewide score is sil… | — |
| 074 | Campaign Target Setting | PARTIAL | POST /pre-election/strength/targets at apps/api/src/routes/pre-election.ts L1977-2006 is restricted to SUPER_ADMIN and STATE_OFFICER and additionally enforces canViewCommandTerritory on the target territory (L1982-1984); it validates endDate > startDate and persists TerritoryTarget (schema.prisma L… | apps/api/src/pre-election.test.ts L515-526 creates a WARD target as SUPER_ADMIN and asserts 201; L539-543 asserts the target then drives progress with actualValue 1. | Of the seven target dimensions named, only verified members, referrals (QUALIFIED_REFERRALS), coordinators (COORDINATOR_COVERAGE) and Polling Unit coverage have an actual-value calculator; tasks, field activity and readiness have none - calculateMetricActual (L668-711) falls through to the last sto… | — |
| 075 | Target vs Actual Tracking | PARTIAL | GET /pre-election/strength/targets/progress (apps/api/src/routes/pre-election.ts L2124-2159) returns targetValue, actualValue, percentageAchieved and shortfall per target, authorization-gated by canViewCommandTerritory; /pre-election/strength/dashboard (L2187-2201) recomputes the same four fields l… | apps/api/src/pre-election.test.ts L539-543 asserts the progress endpoint returns the correct actualValue against the configured target. | The command dashboard matches targets to tiles by exact metric string (dashboard.ts L301, L313), but the tile keys COORDINATORS, POLLING_UNITS and COVERED_POLLING_UNITS never match the strength engine's metric names COORDINATOR_COVERAGE and POLLING_UNIT_COVERAGE, so coordinator and coverage targets… | — |
| 076 | Progress Trend Analytics | PARTIAL | Historical snapshots are persisted per calculation: TerritoryStrengthSnapshot and TerritoryMetricSnapshot with calculatedAt and time-scoped indexes (packages/database/prisma/schema.prisma L1144-1166); apps/api/src/routes/pre-election.ts L2041-2050 and L2075-2084 write both on each calculate. Trend … | None. Grepping IMPROVING/DECLINING/trend across apps/api/src/*.test.ts and apps/worker/src/*.test.ts returns no matches - no test creates two snapshots or asserts a trend classifi… | Nothing captures snapshots on a schedule: the only producer is the manual POST /pre-election/strength/snapshots/calculate, and the background job catalogue (packages/shared/src/job-contracts.ts BACKGROUND_JOB_NAMES) contains only evidence.derivatives.generate, realtime.outbox.replay and maintenance… | — |
| 077 | Strength Heatmaps and Drill-Down | PARTIAL | Bands are defined once with inclusive thresholds and colours in packages/shared/src/dashboard-contracts.ts L90-102 (STRONG 75+, MODERATE 50+, WEAK 25+, CRITICAL 0) and applied server-side to both the current territory and every child (apps/api/src/routes/dashboard.ts L448, L490). apps/web/app/admin… | apps/api/src/pre-election.test.ts L686-690 asserts every child at every level carries a valid band and the correct child level so the grid is always colourable; L694-705 asserts d… | None for the stated scope. (Nice-to-have only: the persisted per-metric breakdownJson from the snapshot engine is returned by the calculate endpoint but not re-exposed on GET /dashboard.) [challenged: The feature is "Authorized officers can identify strong, moderate, weak or critical organizational… | — |
| 078 | Coordinator Performance and Leaderboards | PARTIAL | apps/api/src/routes/dashboard.ts leaderboardFor L327-401 ranks active coordinators in the requested territory, computing VERIFIED_REFERRALS and REGISTERED_MEMBERS from Referral groupBy, TASK_COMPLETION from FieldTask where status=DONE, and FIELD_ACTIVITY from AgentActivity, then sorts and assigns r… | apps/api/src/pre-election.test.ts L714-727 asserts the Ward leaderboard returns rank 1 for the expected coordinator with metric VERIFIED_REFERRALS and value 2, matching the two qu… | Two of the six advertised metrics are not implemented as described. In the fallback branch (dashboard.ts L376-385) COVERAGE passes type: undefined to the AgentActivity groupBy, so COVERAGE returns the total activity count - byte-for-byte the same number as FIELD_ACTIVITY - and neither reflects Poll… | — |
| 079 | Field Task Management | PARTIAL | FieldTask model with FieldTaskStatus TODO/IN_PROGRESS/BLOCKED/DONE (packages/database/prisma/schema.prisma L136-141, L1511-1554) carrying title, description, priority, dueAt, completedAt, resolutionNote and full territory columns. Routes: POST /admin/tasks (apps/api/src/routes/admin.ts L4512-4558) … | None. No test file in apps/api/src/*.test.ts exercises /admin/tasks or /agent/tasks (grep for admin/tasks in the test suites returns no matches); the only FieldTask assertion anyw… | Authorization excludes the actual command roles: /admin/tasks is requireRole("ADMIN","SUPER_ADMIN") only (admin.ts L4512), so STATE_OFFICER and COORDINATOR - the Ogun officer roles per the UserRole enum - cannot create or update tasks at all. Assignment is restricted to users holding an agentProfil… | — |
| 080 | Bulk Task Assignment | PARTIAL | POST /admin/tasks/bulk at apps/api/src/routes/admin.ts L4560-4652 accepts either explicit agentUserIds or a territory filter spanning stateId, senatorialDistrictId, federalConstituencyId, lgaId, wardId, stateConstituencyId and pollingUnitId; it applies the actor's own scope (applyActorAdminScope L4… | None. No test exercises /admin/tasks/bulk. | Same role gap as 079 - requireRole("ADMIN","SUPER_ADMIN") at L4560 excludes STATE_OFFICER and COORDINATOR. Bulk targets only users with an agentProfile (L4596-4617), so "coordinators" and "other permitted territory groups" named in the feature cannot be bulk-assigned. Creation loops one task at a t… | — |
| 081 | Pre-Election Field Activity Logging | PARTIAL | AgentActivityType in packages/database/prisma/schema.prisma L91-99 already enumerates VOTER_OUTREACH, MATERIAL_DISTRIBUTION, OBSERVATION and INCIDENT_RESPONSE alongside CHECK_IN/CHECK_OUT/LOCATION_PING, and the same list is mirrored in packages/shared/src/index.ts L61-68. Check-in/check-out/locatio… | apps/api/src/election-day.test.ts covers Election-Day check-in and the resulting Situation Room totals, but nothing exercises pre-election outreach, observation or material-distri… | Three of the five activity kinds the feature names have no create path anywhere: grepping VOTER_OUTREACH / MATERIAL_DISTRIBUTION / OBSERVATION across apps and packages only hits the enum declarations in schema.prisma and packages/shared/src/index.ts - no route ever writes them. Both existing writer… | — |
| 082 | Coverage Intelligence | PARTIAL | GET /admin/coverage-insights (apps/api/src/routes/admin.ts L5422-5749) computes per-Polling-Unit assignedAgentCount, targetAgentCount, remainingAgentCount, recentActivityCount (24h window, L5423), openIncidentCount, hasAssignedAgent, hasRecentActivity and requiresAttention (L5597-5641), rolls them … | None. No test in apps/api/src/*.test.ts calls /admin/coverage-insights or /admin/polling-unit-coverage (the only "coverage" match in the suites is an unrelated TURN-relay comment … | Both coverage endpoints are requireRole("ADMIN","SUPER_ADMIN") (L5337, L5422), so STATE_OFFICER and COORDINATOR cannot read coverage intelligence. Coverage is measured from legacy agentProfile.pollingUnitId assignments and legacy LGA-shaped scope helpers, not from CoordinatorProfile, so "coordinato… | — |
| 083 | Notifications | PARTIAL | Durable Notification model with userId/type/isRead indexes (packages/database/prisma/schema.prisma L126-134, L1858-1870); createNotification helper (apps/api/src/lib/notifications.ts) used at 20 call sites. Covered events: reward earned (apps/api/src/lib/rewards.ts L57-64), reward redemption approv… | Only apps/api/src/phase1-architecture.test.ts L622 asserts a notification row survives the Phase-1 migration. No test asserts that any business event produces the correct notifica… | Four of the eight event classes the feature names never produce a notification. apps/api/src/routes/pre-election.ts contains no notification code at all (grep for Notification in that file returns nothing), so verification approve/reject/resubmission-required (L1124-1228), referral qualification, R… | — |
| 084 | Operational Broadcasts | PARTIAL | Two paths exist. (1) POST /admin/broadcasts (apps/api/src/routes/admin.ts L4805-4911) with a matching dry-run POST /admin/broadcasts/preview (L4745-4803): it validates territory references, enforces isWithinAdminScope, validateBroadcastTargeting and validateBroadcastPartyScope, resolves recipients,… | apps/api/src/election-day.test.ts L276-292 sends a territory message as a State Officer and asserts the Polling Unit Coordinator receives it via the REST fallback. No test covers … | The full-featured broadcast path is requireRole("ADMIN","SUPER_ADMIN") (admin.ts L4745, L4805), so STATE_OFFICER and COORDINATOR cannot use audience targeting or the preview. The command-scope path that they can use has no UI at all - grepping messages/territory across apps/web returns nothing - an… | — |
| 085 | Broadcast History | PARTIAL | BroadcastMessage persists creator (createdByUserId), audience, taskStatus, content, createdAt, recipientCount and the full territory column set including senatorialDistrictId/federalConstituencyId/stateConstituencyId/pollingUnitId (packages/database/prisma/schema.prisma L1556-1590). GET /admin/broa… | apps/api/src/election-day.test.ts L287-290 reads back a previously sent broadcast through GET /election-day/messages and asserts its title, which is the only history assertion in … | Recipient scope is stored only as an integer recipientCount - there is no BroadcastRecipient join table in schema.prisma, so who actually received a given broadcast is not recoverable, and the per-recipient Notification rows created at admin.ts L4869-4876 carry no broadcastId back-reference. No del… | — |
| 086 | Member Dashboard | PARTIAL | apps/web/app/dashboard/page.tsx is a working member view loading the profile, pre-election verification case, notifications, rewards and events in one pass (L87-131). It shows name and referral code (L299-303), verification status as a pill plus reviewer note and fraud flag (L341-352), a resubmissi… | apps/api/src/pre-election.test.ts L309-323 exercises the member resubmission path end to end and asserts 201; the surrounding suite covers member registration and the verification… | Territory is not displayed - the page never renders the member's state / senatorial district / federal constituency / state constituency / ward / Polling Unit; the only territorial reference is a count of "live events in your territory" (L306-309). There is no announcements section distinct from no… | — |
| 087 | Search, Filters and Management Views | PARTIAL | The verification queue is the strongest surface: GET /pre-election/verifications (apps/api/src/routes/pre-election.ts L923-1005) accepts territoryType/territoryId across the whole command hierarchy with a paired-parameter check and a canViewCommandTerritory authorization check (L928-937), plus stat… | apps/api/src/pre-election.test.ts exercises the verification queue and referral listing used by these views; apps/api/src/admin-global-structures.test.ts covers scoped user manage… | The management user view is missing most of the required filter axes: managedUsersQuerySchema (admin.ts L333-341) accepts only role, isActive, search, stateId, lgaId, wardId and limit - there is no senatorialDistrictId, federalConstituencyId, stateConstituencyId or pollingUnitId filter, and no veri… | — |
| 088 | Reporting and Export | PARTIAL | Three controlled export paths exist. GET /admin/voters/export (apps/api/src/routes/admin.ts L3174-3233) is SUPER_ADMIN-only, restricted to members who gave contactConsent, deliberately excludes voterCardNumber and voter documents, CSV-escapes every field, and writes a VOTER_CONTACT_EXPORT audit ent… | apps/api/src/evidence.test.ts L350 asserts audit rows are written for evidence access/export actions. No test covers /admin/voters/export or the verification CSV export. | Six of the eight report domains named have no export at all - grepping text/csv across apps/api/src returns exactly two endpoints, so there is no referral, reward, payout, strength, coverage or campaign-performance report. Only CSV is produced: no Excel/xlsx generation exists anywhere in the repo, … | — |
| 089 | Mobile-First Production Architecture | PARTIAL | The web app is Next.js App Router (apps/web/app/layout.tsx) so a default width=device-width viewport is emitted. Several key surfaces use fluid layouts that reflow on narrow screens: apps/web/app/globals.css L1575-1580 (.command-tiles auto-fit minmax 180px), L1620-1625 (.command-heatmap auto-fill m… | None. There is no viewport, responsive, device or visual test anywhere in the repo (the eight test files under apps/api/src and apps/worker/src are all API/worker integration suit… | Field-device readiness is not established. layout.tsx exports metadata only - no explicit viewport export and no theme-color. There is no PWA surface at all: no apps/web/public/manifest.json, no service worker, no offline queue or retry for the field write paths (check-in, evidence upload, election… | — |
| 090 | Audit and Security Logging | PARTIAL | apps/api/src/lib/audit.ts createAuditLog writes actor, action, targetType, targetId and a JSON metadata blob that normalises the full territory (L31-42) and political party, persisted to AuditLog (packages/database/prisma/schema.prisma L1873-1885) with indexes on actor, action and createdAt. 63 cal… | apps/api/src/evidence.test.ts L350 asserts audit rows are created for evidence actions; apps/api/src/phase1-architecture.test.ts L623 asserts audit history survives the Phase-1 us… | Authentication - explicitly named first in the feature - is never audited: apps/api/src/routes/auth.ts contains no createAuditLog call for POST /login (L88), POST /logout (L148) or POST /register-voter (L309), so successful logins, failed login attempts, logouts and session revocations produce no d… | — |
| 091 | Election Situation Room | PARTIAL | apps/api/src/routes/election-day.ts:1296 GET /situation-room/status → requireCommandScope() (election-day.ts:260) resolves the actor's command territory and gates it with authorizeAction(actor,"VIEW_TERRITORY",resolved); buildSituationRoomStatus (election-day.ts:763-999) assembles PU statuses, tota… | apps/api/src/election-day.test.ts:252 "Situation Room status is durable, territory scoped, and denies members" asserts expectedPollingUnits=2, assignedPollingUnits=2, checkedInPol… | Not a real-time command interface: apps/web/package.json has no socket.io-client and there is zero reference to socket.io anywhere in apps/web, so the page refreshes on a 30s setInterval (situation-room/page.tsx:128-137). The ADMIN role is locked out — commandScopeForActor (election-day.ts:196-213)… | — |
| 092 | Real-Time Election Statistics | PARTIAL | buildSituationRoomStatus (apps/api/src/routes/election-day.ts:763-999) computes totals for check-ins, incidents/criticalIncidents, reportsReceived/Outstanding, resultReportsReceived, evidenceReceived, completedPollingUnits, reportingPercentage, plus byOpeningStatus/byReportStatus breakdowns. Events… | apps/api/src/realtime.test.ts:256 publishes election.incident.created and asserts state and ward-A sockets receive it while ward-B does not. apps/api/src/election-day.test.ts:252 … | No browser consumes the stream — statistics are 30s HTTP polling only. Durability is inconsistent: only alerts, messages and calls go through persistAndPublishRealtimeEvent/the outbox (election-day.ts:472-507,1376,1486,2452,2554 and publishCallEvent:1796); check-in, check-out, location, incident, r… | — |
| 093 | Hierarchical Real-Time Statistics | PARTIAL | commandScopeForActor (apps/api/src/routes/election-day.ts:196-213), scopeToWhere (215-229) and territoryFilter (231-240) restrict every Situation Room query to the actor's own subtree, so a Ward Coordinator sees only its Wards' PUs and a STATE_OFFICER sees Ogun-wide. Realtime rooms are cut at every… | apps/api/src/realtime.test.ts:256 proves ward-level room isolation between two State Constituencies and denies a MEMBER subscription. apps/api/src/election-day.test.ts:363 proves … | The endpoint returns a flat pollingUnits[] array plus one totals block — it never produces child-level rollups, so a State Constituency does not 'see subordinate Wards' as aggregated rows and a Senatorial District does not see subordinate Federal Constituencies; it just sees all leaf PUs at once. s… | — |
| 094 | Live Drill-Down | PARTIAL | apps/api/src/routes/dashboard.ts:403 GET / accepts level+territoryId over all six levels (DASHBOARD_LEVELS), maps them to an OperationalTerritory (territoryFor:57-73), authorizes once with authorizeAction(...,"VIEW_TERRITORY") at line 421 so drilling cannot cross into a sibling, and returns breadcr… | None. There is no test file covering apps/api/src/routes/dashboard.ts; the seven suites in apps/api/src/run-tests.ts do not exercise the hierarchical dashboard at all. | The drill-down that exists is the pre-election membership-strength dashboard, not the live Election Day board: its tiles are registered/verified members, coordinators and PU counts (dashboard.ts:296-321), and pollingUnitDetail collapses operational state to CHECKED_IN/NOT_CHECKED_IN with lifetime (… | — |
| 095 | Polling Unit Operational Status | PARTIAL | All nine statuses are declared in packages/shared/src/platform-contracts.ts:80-90 (POLLING_UNIT_OPERATIONAL_STATUSES) and typed onto ElectionDayPollingUnitStatus.operationalStatus (platform-contracts.ts:393). operationalStatusFor (apps/api/src/routes/election-day.ts:739-761) derives a status per PU… | apps/api/src/election-day.test.ts:212 asserts only checkedInAt and the geofence status on GET /my-status. No test asserts any operationalStatus value or a transition between value… | Only six of the nine statuses can ever be produced: OPENED, COUNTING and UNDER_REVIEW have no producer anywhere (grep over apps/ finds no assignment) — a report with status UNDER_REVIEW still maps to REPORTING, and there is no opening or counting event to raise OPENED/COUNTING. Status is recomputed… | — |
| 096 | Live Election Operations Map | PARTIAL | apps/api/src/routes/admin.ts:4991 GET /map-summary returns activeAgents (latest activity lat/long, filtered by canCreateAgentInScope), geolocated incidents and scoped pollingUnits. UI: apps/web/app/admin/operations/live/page.tsx renders GoogleLiveMap with a RasterLiveMap OSM-tile fallback (507-516)… | None. No API or UI test covers /admin/map-summary or the map components. | buildLiveMapMarkers (live/page.tsx:53-79) plots only agents and incidents — the pollingUnits array returned by the API is never turned into markers, and operational status, missing reports and tracking alerts appear nowhere on the map. PU coordinates are unavailable anyway (authoritative PU geodata… | — |
| 097 | Real-Time Alert System | PARTIAL | OperationalAlert model with status/severity/territory/source/lifecycle timestamps at packages/database/prisma/schema.prisma:1592-1626. apps/api/src/routes/election-day.ts:1311 POST /alerts/reconcile creates de-duplicated open alerts via upsertOpenAlert (533-583), 1400 GET /alerts filters by status/… | apps/api/src/election-day.test.ts:294 "alert reconciliation creates durable alert lifecycle events" reconciles, reads back a NO_CHECK_IN alert, PATCHes it to ACKNOWLEDGED and asse… | Only three of the seven declared alert types are ever created — NO_CHECK_IN, REPORT_OVERDUE and LOCATION_STALE (election-day.ts:1327-1372). TRACKING_STOPPED, LOCATION_MISMATCH, GPS_PERMISSION_DISABLED and DEVICE_SESSION_CHANGED exist in ELECTION_DAY_ALERT_TYPES (packages/shared/src/platform-contrac… | — |
| 098 | Polling Unit Coordinator Check-In | PARTIAL | apps/api/src/routes/election-day.ts:1069 POST /check-in behind requireAuth + requirePollingUnitFieldCapability (apps/api/src/middleware/auth.ts:86-95). createFieldActivity (election-day.ts:633-733) takes identity from the token, rejects a pollingUnitId that is not the coordinator's assignment (647-… | apps/api/src/election-day.test.ts:212 "PUC check-in is idempotent and geofence evaluation remains gated" asserts 201 then 200 with alreadyRecorded=true for a repeated idempotencyK… | The shipped UI never calls this endpoint. apps/web/app/agent/dashboard/page.tsx:322 calls createAgentActivity(token,"check-in"), which posts to /agent/check-in (apps/web/lib/api.ts:2758-2778); grep over apps/web finds no reference to /election-day/check-in. The legacy route apps/api/src/routes/agen… | — |
| 099 | Election-Day GPS Tracking | PARTIAL | apps/api/src/routes/election-day.ts:1136 POST /location-pings requires an AgentProfile with gpsTrackingConsentAt (election-day.ts:643-645) and rejects a ping without coordinates (655-660); AgentActivity persists latitude/longitude/accuracyMeters/createdAt (packages/database/prisma/schema.prisma:143… | apps/api/src/election-day.test.ts:238 "location pings fail closed without GPS tracking consent" asserts 400 with /GPS tracking consent is required/ after clearing gpsTrackingConse… | The tracker posts to /agent/location (agent-session-tracker.tsx:92 → apps/web/lib/api.ts:2769), not /election-day/location-pings — apps/api/src/routes/agent.ts:237-249 performs no consent re-check, writes no audit log and publishes no election.location.updated event, so the tested hardened path is … | — |
| 100 | Polling Unit Geofence Monitoring | BLOCKED | PollingUnit carries latitude, longitude, geoAccuracyMeters, geoCaptureMethod, geoCapturedAt, geoSource, geofenceRadiusMeters and geodataImportReleaseId (packages/database/prisma/schema.prisma:2530-2556). The contract declares ELECTION_DAY_GEOFENCE_STATUSES including IN_RANGE / OUT_OF_RANGE_REVIEW_R… | apps/api/src/election-day.test.ts:212 asserts the check-in response and GET /my-status both report geofence.status === "GATED_AUTHORITATIVE_PU_GEODATA_REQUIRED". | Authoritative Ogun Polling Unit latitude/longitude/radius data is MISSING, so the expected location the comparison needs does not exist. Note this is not code-complete-pending-data: no distance computation exists anywhere in the repo (a grep for haversine/distanceMeters producers finds only the har… | EXTERNAL_DATA |
| 101 | Tracking Loss and Stale-Location Alerts | PARTIAL | apps/api/src/routes/election-day.ts:1359-1372 creates a durable LOCATION_STALE alert when a PU's lastSeenAt is older than a 45-minute threshold (staleThresholdMs, line 1323), tagged with lastSeenAt/staleThresholdMinutes metadata, and publishes it as election.tracking.stale (1378). severityForAlert … | apps/api/src/election-day.test.ts:294 exercises reconciliation but its fixtures never produce a stale-location case, so only the NO_CHECK_IN branch is actually asserted. No test c… | Two of the four spec conditions have no producer at all: nothing ever creates a TRACKING_STOPPED or GPS_PERMISSION_DISABLED alert. When the browser loses GPS permission the tracker silently force-logs-out the coordinator (apps/web/components/agent-session-tracker.tsx:124-126 → forceLogout('gps-requ… | — |
| 102 | Location Mismatch Alerts | BLOCKED | LOCATION_MISMATCH is declared in ELECTION_DAY_ALERT_TYPES (packages/shared/src/platform-contracts.ts:104) and election.location.mismatch in ELECTION_DAY_REALTIME_EVENT_TYPES (platform-contracts.ts:121); ElectionDayLocationEvaluation reserves distanceMeters and accuracyMeters (platform-contracts.ts:… | None. No test creates or asserts a LOCATION_MISMATCH alert; election-day.test.ts only asserts the gated evaluation. | The alert requires an expected location and a distance, which depend on authoritative PU coordinates and radius that are MISSING. Beyond that data gap, no code exists: grep finds LOCATION_MISMATCH only in the enum at platform-contracts.ts:104 and in the cast at election-day.ts:948 — nothing constru… | EXTERNAL_DATA |
| 103 | Tracking Escalation Workflow | PARTIAL | Alerts accept an ESCALATED lifecycle state that stamps escalatedAt, writes an ELECTION_DAY_ALERT_STATUS_UPDATED audit log and emits a durable election.alert.updated event (apps/api/src/routes/election-day.ts:1426-1504); OperationalAlert.escalatedAt is persisted (packages/database/prisma/schema.pris… | None for escalation routing. election-day.test.ts:294 only PATCHes an alert to ACKNOWLEDGED; no test escalates an alert or an incident, and no test asserts any level-to-level rout… | There is no escalation chain — nothing computes the next level up (PU→Ward→State Constituency→Federal Constituency→Senatorial District→General State Officer), nothing reassigns an escalated alert or incident to a superior coordinator, and organization-tree.ts is never called from any escalation pat… | — |
| 104 | Quick Communication from Alerts and Map | PARTIAL | apps/web/app/election-day/situation-room/page.tsx:395-413 gives a selected Polling Unit a Message button (openDirectChat, 201-221), a Request Check-In button that opens a DIRECT conversation and posts a templated message with quickAction/pollingUnitId metadata (223-247), and a View Reports link. Ca… | apps/api/src/election-day.test.ts:325 covers the DIRECT-conversation and message APIs those buttons use, and 454/540/587 cover the call APIs. No test covers the quick-action surfa… | None of these actions are available from an alert: the alert card exposes only Acknowledge/Escalate/Resolve (situation-room/page.tsx:455-459). None are available from the map: apps/web/components/google-live-map.tsx and raster-live-map.tsx render points with no click actions, and apps/web/app/admin… | — |
| 105 | Missing-Report Contact Actions | PARTIAL | reportStatus, reportsReceived and reportsOutstanding are computed per PU and in aggregate (apps/api/src/routes/election-day.ts:905,922-926); a durable REPORT_OVERDUE alert is created for any PU that checked in but has not submitted (election-day.ts:1344-1357). The Situation Room PU board prints 'Re… | None specific to missing reports. election-day.test.ts:294 asserts only the NO_CHECK_IN alert from reconciliation; no test creates a REPORT_OVERDUE case or exercises a contact act… | There is no list of outstanding Polling Unit reports — the operator must locate the unit inside an unfiltered 80-item board (situation-room/page.tsx:376) and click it; there is no filter, sort or dedicated outstanding-reports view. The REPORT_OVERDUE alert card itself carries no contact action. The… | — |
| 106 | Operational Presence Indicators | PARTIAL | RealtimePresenceState covers ONLINE | ACTIVE_RECENTLY | OFFLINE | IN_CALL (packages/shared/src/platform-contracts.ts:362). apps/api/src/realtime/gateway.ts maintains per-user presence rooms and socket counts (101-134), emits presence.updated, mirrors entries to Redis with a TTL (114-128) and accept… | None. Neither realtime.test.ts nor election-day.test.ts asserts any presence state, the heartbeat handler, or the /election-day/presence endpoint. | Nothing renders presence: apps/web/lib/api.ts:2367 fetchElectionDayPresence has zero callers, and the Situation Room conversation list and message pane display no presence badge (situation-room/page.tsx:476-499). getPresenceSnapshot reads only the in-process localPresence Map (gateway.ts:436-447) a… | — |
| 107 | One-to-One In-App Messaging | PARTIAL | Conversation, ConversationMember, Message, MessageReceipt and MessageAttachment models with unique(conversationId,userId) and unique(messageId,userId,status) (packages/database/prisma/schema.prisma:1629-1725). apps/api/src/routes/election-day.ts:2279 POST /conversations gates DIRECT on canMessageUs… | apps/api/src/election-day.test.ts:325 "direct Election Operations messaging is permission scoped and durable": a STATE_OFFICER opens a DIRECT conversation with a PUC, sends a mess… | Delivery and read status are never shown — message bubbles render sender, body and time only (situation-room/page.tsx:493-499) even though receipts are serialized. Incoming messages do not appear: the message-loading effect depends only on selectedConversationId (situation-room/page.tsx:139-148), s… | — |
| 108 | Group and Territory Messaging | PARTIAL | ConversationType covers DIRECT/GROUP/TERRITORY/ELECTION_OPERATION (packages/database/prisma/schema.prisma:336-341). apps/api/src/routes/election-day.ts:2312-2332 resolves a requested territory, re-authorizes it with authorizeAction(...,"VIEW_TERRITORY") and auto-enrolls the active coordinators in s… | apps/api/src/election-day.test.ts:276 "territory messaging foundation persists REST fallback messages for PUC recipients" — a STATE_OFFICER posts a territory message and the PUC r… | The UI creates exactly one kind of group: an ELECTION_OPERATION conversation pinned to the actor's own scope (apps/web/app/election-day/situation-room/page.tsx:249-270) — there is no picker for Ward, State Constituency, Federal Constituency, Senatorial District or State operations groups, and the G… | — |
| 109 | Messaging Permission Rules | COMPLETE | apps/api/src/lib/messaging-permissions.ts is the single shared rule: messagingCommandScope (15-43) derives scope from role, contactTerritoryForUser (46-76) returns null for any account that is not isActive with accountStatus ACTIVE, and canContactUser (83-98) resolves both territories and permits c… | apps/api/src/election-day.test.ts:325 denies a MEMBER posting into a conversation (403); :587 denies a PU coordinator calling a coordinator in another State Constituency (403), de… | None material. All four spec inputs — role, territory, organizational relationship and account status — are enforced server-side on every messaging and calling entry point, in both transports, with direct negative-path test evidence. | — |
| 110 | In-App Voice Calling | PARTIAL | apps/api/src/routes/election-day.ts:1653 GET /webrtc/config returns STUN from env.WEBRTC_STUN_URLS and adds a TURN entry only when the URI matches a turn:/turns: pattern and both credentials are present (1662-1673), reporting turnConfigured honestly. Signalling exists on both transports: apps/api/s… | apps/api/src/election-day.test.ts:407 asserts the config contract (socket.io transport, call.* events, >=1 STUN entry, turnConfigured=false when unset, recording DISABLED) and tab… | No call can carry audio: a grep over apps/web finds no createOffer, createAnswer, setLocalDescription, setRemoteDescription, onicecandidate or addIceCandidate anywhere, and apps/web/package.json has no socket.io-client, so VoiceCallPanel builds a peer connection that never negotiates and never rece… | — |
| 111 | Call Permissions, Interface and History | PARTIAL | VoiceCall, VoiceCallParticipant and VoiceCallEvent give durable lifecycle and history with recordingPolicy DISABLED (packages/database/prisma/schema.prisma:1727-1805). apps/api/src/routes/election-day.ts implements initiate (1835, with canContactUser, self-call rejection, and a 409 when a live call… | apps/api/src/election-day.test.ts:454 walks INITIATED→RINGING→ACCEPTED→CONNECTED→ENDED, verifies the durable VoiceCall row, participants, >=5 events and that a terminal call canno… | The callee is never told a call is ringing: VoiceCallPanel calls refresh() only on mount and after its own actions (voice-call-panel.tsx:76-86) with no interval and no socket subscription, so an incoming RINGING call is not surfaced until the page is reloaded. The Answer/Decline gate at voice-call-… | — |
| 112 | Election Operations Chat | PARTIAL | ELECTION_OPERATION is a first-class ConversationType (packages/database/prisma/schema.prisma:336-341) created from the Situation Room bound to the current command scope (apps/web/app/election-day/situation-room/page.tsx:249-270 → apps/api/src/routes/election-day.ts:2312-2332, which re-authorizes th… | None specific. election-day.test.ts:325 covers DIRECT conversations only; no test creates an ELECTION_OPERATION conversation or asserts its appearance in the timeline. | TERRITORY and ELECTION_OPERATION share one identical code branch (election-day.ts:2312-2332) — the type carries no distinct behaviour or membership rule. There is no linkage from a chat to the workflow object it concerns: Conversation has no incidentId/alertId/electionReportId column, so 'integrate… | — |
| 113 | Incident Reporting | PARTIAL | IncidentType covers VIOLENCE, INTIMIDATION, VOTE_BUYING, MATERIAL_SHORTAGE, LOGISTICS_DELAY, MALFUNCTION, SECURITY_CONCERN, OTHER (packages/database/prisma/schema.prisma:101-110) — all seven spec categories. apps/api/src/routes/election-day.ts:1171 POST /incidents is behind requirePollingUnitFieldC… | No test submits an incident. apps/api/src/election-day.test.ts contains no incident-creation case; apps/api/src/realtime.test.ts:256 only synthesizes an election.incident.created … | The UI posts to the weaker legacy route: apps/web/lib/api.ts:2781 createAgentIncident targets /agent/incidents (apps/api/src/routes/agent.ts:285-347), which writes no audit log, publishes no realtime event, and accepts a client-supplied senatorialDistrictId into the Incident row (agent.ts:322) inst… | — |
| 114 | Incident Severity and Workflow | PARTIAL | IncidentSeverity LOW/MEDIUM/HIGH/CRITICAL and IncidentStatus OPEN/IN_PROGRESS/RESOLVED/CLOSED are exactly the spec enums (packages/database/prisma/schema.prisma:112-125), persisted on Incident (1471) and validated by zod nativeEnum on submission (apps/api/src/routes/election-day.ts:72-80). apps/api… | None. No test in apps/api/src/*.test.ts changes an incident status or asserts a severity-driven behaviour. | There is no UI to move an incident through the workflow: apps/web/app/admin/incidents/page.tsx is entirely read-only (filters and governance flags, no action buttons) and apps/web/lib/api.ts has no status-update client function — grep for updateAdminIncident/IncidentStatus in apps/web returns nothi… | — |
| 115 | Incident Multimedia Evidence | PARTIAL | Incident carries latitude/longitude, createdAt, reportedByUserId, pollingUnitId and relations to both mediaAttachments and evidenceAssets (packages/database/prisma/schema.prisma:1464-1509). EvidenceType supports PHOTO, VIDEO and WRITTEN_REPORT (schema.prisma:254-258) and apps/api/src/routes/evidenc… | apps/api/src/evidence.test.ts:221 "PUC uploads incident evidence with server SHA-256, unique object key, metadata, and custody" exercises the incident-linked upload path end to en… | Field coordinators have no way to attach anything to an incident from the app: the only evidence-upload UI is apps/web/app/admin/evidence/page.tsx:237-262 (ADMIN surface), and neither incident-creation route accepts an attachment — incidentSchema in apps/api/src/routes/election-day.ts:72-80 and app… | — |
| 116 | Incident Assignment and Escalation | PARTIAL | apps/api/src/routes/admin.ts:4160 PATCH /incidents/:id/assign validates that both the actor and the assignee are within the incident's scope (4183-4194), notifies the assignee and audits; :4225 PATCH /incidents/:id/escalate stamps escalatedAt/escalatedByUserId/escalationNote and audits; :4109 PATCH… | None. No test assigns, escalates, resolves or closes an incident. | Every one of these APIs is unreachable from the UI: apps/web/app/admin/incidents/page.tsx renders a read-only queue and apps/web/lib/api.ts exposes no assign/escalate/status client function. 'Add notes' has no repeatable mechanism — there is no IncidentNote model, only the single overwritable escal… | — |
| 117 | Structured Election-Day Reporting | PARTIAL | electionDayReportSchema (apps/api/src/routes/agent.ts:73-92) requires reportDate, openingStatus, arrivalConfirmedAt, turnoutObservation, optional incidentNotes and remarks, two photo asset ids and exactly five distinct-party vote entries. POST /agent/election-reports (agent.ts:444) verifies asset o… | None. No test submits an election-day report; apps/api/src/phase1-architecture.test.ts:298 only creates an ElectionDayReport row as a migration fixture. | The result section is hard-coded to exactly five parties (z.array(...).length(5), agent.ts:82), so a Polling Unit contesting a different number of parties cannot file a report. 'Incidents' is a free-text incidentNotes string with no link to actual Incident records. 'Completion information' has no f… | — |
| 118 | Pictorial, Video and Information Proof | PARTIAL | The evidence subsystem models all three forms — EvidenceType PHOTO | VIDEO | WRITTEN_REPORT (packages/database/prisma/schema.prisma:254-258) — and validateEvidenceContent (apps/api/src/routes/evidence.ts:463-480) enforces image/jpeg|png|webp for PHOTO, video/mp4|webm|quicktime for VIDEO and text/pl… | apps/api/src/evidence.test.ts:221 and :296 cover the upload, hashing, custody and derivative-reporting path. No test uploads VIDEO or WRITTEN_REPORT evidence from an Election Day … | Election Day reporting itself supports only pictorial proof: ElectionDayReportAssetKind is limited to ARRIVAL_PHOTO and POST_COUNTING_PHOTO (schema.prisma:188-191) and the upload route accepts only image/jpeg|png|webp (apps/api/src/routes/agent.ts:63-66,367). Video and written proof exist only in t… | — |
| 119 | Live Result and Reporting Monitoring | PARTIAL | buildSituationRoomStatus totals cover reportsReceived, reportsOutstanding, reportingPercentage, resultReportsReceived, evidenceReceived and completedPollingUnits, plus a byReportStatus map that carries review status (apps/api/src/routes/election-day.ts:922-926, 973-995). Per-PU flags reportStatus, … | apps/api/src/election-day.test.ts:252 asserts the check-in and PU totals but asserts none of the reporting/result/evidence totals. No test covers /admin/election-day-reports. | Four of the eight monitored quantities are computed but never displayed: the Situation Room renders only six tiles — Expected PUs, Checked In, Reports %, Open Alerts, Critical Incidents, Stale Tracking (apps/web/app/election-day/situation-room/page.tsx:339-364) — so reportsOutstanding, resultReport… | — |
| 120 | Report Review and Operational Timeline | PARTIAL | apps/api/src/routes/admin.ts:4376 PATCH /election-day-reports/:reportId/status enforces scope with canCreateAgentInScope plus a party check (4399-4412), stores status/reviewNote/reviewedByUserId/reviewedAt, notifies the reporter and audits, then emits election.report.reviewed and election.situation… | None. No test covers the review endpoint or GET /timeline; apps/api/src/election-day.test.ts has no timeline case. | Only two of the four reviewer actions exist: ElectionDayReportStatus is SUBMITTED/UNDER_REVIEW/APPROVED/REJECTED (packages/database/prisma/schema.prisma:181-186), so 'request clarification' and 'request correction' have no distinct state — both collapse into a free-text reviewNote that is overwritt… | — |
| 121 | Post-Election Source of Truth | PARTIAL | Reports (ElectionDayReport incl. voteEntriesJson results), incidents (Incident), operational events (AgentActivity), evidence (EvidenceAsset) and timestamps are all persisted in packages/database/prisma/schema.prisma (models at lines 1464 Incident, 1907 ElectionDayReport, 1982 EvidenceAsset) with r… | apps/api/src/evidence.test.ts case 'PU timeline and dossier include evidence, incidents, hashes, and no legal conclusion' asserts INCIDENT + EVIDENCE entries and dossier completen… | The agent's primary Election Day artifacts (arrival photo, post-counting photo) are uploaded at apps/api/src/routes/agent.ts:373 into ElectionDayReportAsset as raw Postgres Bytes with no SHA-256, no custody events, no classification/review status, and no EvidenceAsset record — so the canonical fiel… | — |
| 122 | Three Evidence Types | PARTIAL | EvidenceType enum PHOTO/VIDEO/WRITTEN_REPORT at packages/database/prisma/schema.prisma:254, EvidenceAsset.evidenceType column and index; EVIDENCE_TYPES in packages/shared/src; per-type MIME validation in apps/api/src/routes/evidence.ts validateEvidenceContent (PHOTO jpeg/png/webp, VIDEO mp4/webm/qu… | apps/api/src/evidence.test.ts exercises PHOTO end-to-end through POST /evidence/uploads/finalize. apps/worker/src/derivatives.test.ts covers VIDEO and WRITTEN_REPORT assets, but c… | VIDEO is not usable end-to-end: uploads are base64 JSON bodies and express.json is capped by API_JSON_BODY_LIMIT (default and .env.production.example value '1mb', apps/api/src/env.ts:67), so the real ceiling is ~750KB of file bytes even though validateEvidenceContent claims 8MB. No API-level test c… | — |
| 123 | Evidence Linked to Events | PARTIAL | apps/api/src/routes/evidence.ts uploadFinalizeSchema.superRefine rejects any upload without pollingUnitId, incidentId, or electionReportId; resolveUploadTarget() resolves and cross-validates the linked incident/report/PU and rejects mismatched Polling Units. FKs enforced in schema.prisma EvidenceAs… | apps/api/src/evidence.test.ts 'PUC uploads incident evidence...' asserts row.incidentId and row.pollingUnitId persisted; explorer/dossier tests query by link. | Minor: incidentId/electionReportId FKs are ON DELETE SET NULL, so evidence attached only to a deleted incident (with no PU) could end up unlinked; no test covers that path. [challenged: The linkage rule is enforced for evidence assets that exist, but the half of the feature that matters operational… | — |
| 124 | Evidence Chain of Custody | COMPLETE | EvidenceCustodyEvent model (schema.prisma:2047) with EvidenceCustodyEventType UPLOADED/VIEWED/REVIEWED/CLASSIFIED/DOWNLOADED/EXPORTED/ADDED_TO_CASE (schema.prisma:282). Events written in apps/api/src/routes/evidence.ts on upload (:712, in the same $transaction as the asset), signed access (:842), r… | apps/api/src/evidence.test.ts asserts the UPLOADED custody event, DOWNLOADED count == 1, CLASSIFIED and REVIEWED events after review, EXPORTED count == 1, and dossier custodyEvent… | Minor: EvidenceCustodyEvent.actorUserId is ON DELETE SET NULL, so custody actor identity can be erased when a user row is deleted; custody events cascade-delete with the asset. | — |
| 125 | Cryptographic Evidence Hashing | COMPLETE | Server computes SHA-256 in apps/api/src/routes/evidence.ts POST /uploads/finalize (crypto.createHash('sha256').update(body)) and rejects with 500 if the stored object hash differs; sha256 column + index on EvidenceAsset (schema.prisma:1982). Verified again on every access: POST /:id/access refuses … | apps/api/src/evidence.test.ts asserts evidence.sha256 equals the client-computed digest, that storage.getObject().sha256 matches, and that the manifest SHA-256 verifies. apps/work… | None material for the hashing requirement. | — |
| 126 | Original Evidence Preservation | PARTIAL | packages/object-storage/src/evidence-storage.ts exposes only putObjectIfAbsent (in-memory driver throws EvidenceObjectAlreadyExistsError; S3 driver sends 'if-none-match: *' and maps 412/409 to the same error). apps/api/src/routes/evidence.ts returns 409 'silent overwrite denied' on conflict, Eviden… | apps/api/src/evidence.test.ts 'storage test double denies overwrites and never uses filesystem authority' and the /evidence/storage/health assertions (originalOverwritePolicy = DE… | All preservation testing runs against InMemoryEvidenceObjectStorage; the S3CompatibleEvidenceObjectStorage path (the only driver production accepts) is never exercised by any test and no bucket exists, so the conditional-write guarantee is unverified against a real store. No bucket versioning, obje… | — |
| 127 | Preview and Streaming Derivatives | PARTIAL | EvidenceDerivative model with kinds THUMBNAIL/PREVIEW/POSTER and statuses PENDING/PROCESSING/READY/FAILED/DEFERRED (schema.prisma:2066, 298, 304) plus migration ogun-migrations/20260810120000_background_jobs_and_evidence_derivatives. Job enqueued durably in the same transaction as the asset (apps/a… | apps/worker/src/derivatives.test.ts (6 cases: generation, idempotent retry, video deferral, written-report skip, hash-mismatch refusal, missing asset). apps/api/src/evidence.test.… | Nothing can ever read a derivative: apps/api/src/routes/evidence.ts only signs asset.originalStorageKey (POST /:id/access), there is no endpoint that returns a derivative URL, and apps/web/app/admin/evidence/page.tsx never renders a thumbnail or preview. GET /evidence (evidence.ts:517) includes onl… | — |
| 128 | Multi-Point Timestamping | PARTIAL | EvidenceAsset carries capturedAt (nullable), uploadedAt, serverReceivedAt (indexed) plus createdAt/updatedAt (schema.prisma:1982). apps/api/src/routes/evidence.ts sets serverReceivedAt server-side and treats it as authoritative: it is the orderBy for the explorer, the aggregation and the PU timelin… | apps/api/src/evidence.test.ts exercises the upload (serverReceivedAt written), and the timeline/explorer cases depend on serverReceivedAt ordering; custody event timestamps assert… | Minor: uploadedAt is server-assigned to the same instant as serverReceivedAt (no client-declared upload time is accepted), and capturedAt is client-supplied without any sanity bound and is not covered by a test. [challenged: Only one of the five timestamp points is genuinely implemented end-to-end.… | — |
| 129 | Evidence Location Context | PARTIAL | EvidenceAsset stores latitude, longitude, accuracyMeters plus the full territory chain (stateId, senatorialDistrictId, federalConstituencyId, lgaId, wardId, stateConstituencyId, pollingUnitId) — schema.prisma:1982. apps/api/src/routes/evidence.ts uploadFinalizeSchema validates coordinate ranges and… | apps/api/src/evidence.test.ts sends latitude 7.1 / longitude 3.2 on the upload and asserts row.pollingUnitId, but never asserts that latitude/longitude/accuracyMeters round-trip. … | No geofence or location-context evaluation of any kind: coordinates are never compared to the Polling Unit, and Ogun PU geodata is MISSING so no reference geometry exists to compare against. No 'where permitted' consent gate on evidence-attached GPS (agent gpsTrackingConsentAt is not consulted by t… | EXTERNAL_DATA |
| 130 | Evidence Classification | COMPLETE | EvidenceClassification enum with exactly the ten locked values ARRIVAL/OPENING/MATERIALS/SECURITY/INCIDENT/VOTING_PROCESS/COUNTING/RESULT_SHEET/POST_COUNTING/OTHER (packages/database/prisma/schema.prisma:260), defaulted to OTHER on EvidenceAsset with an index, created in ogun-migrations/20260809050… | apps/api/src/evidence.test.ts 'review transitions and manual custody events are preserved' asserts classification changes INCIDENT -> SECURITY and that a CLASSIFIED custody event … | None material. | — |
| 131 | Evidence Review Status | COMPLETE | EvidenceReviewStatus enum with exactly SUBMITTED/UNDER_REVIEW/VERIFIED/DISPUTED/REQUIRES_CLARIFICATION/ARCHIVED (schema.prisma:273), default SUBMITTED, indexed. POST /evidence/:id/review (apps/api/src/routes/evidence.ts:895) is gated by isPrivilegedEvidenceReviewer (SUPER_ADMIN/STATE_OFFICER/VALIDA… | apps/api/src/evidence.test.ts asserts the VERIFIED transition, the REVIEWED/CLASSIFIED custody events, and summary.byReviewStatus.VERIFIED >= 1 in the explorer. | Minor: no transition rules (any status can jump to any other, including ARCHIVED -> SUBMITTED), and reviewer identity/review time are only in custody metadata, not columns on EvidenceAsset. | — |
| 132 | Polling Unit Evidence Timeline | PARTIAL | GET /evidence/polling-units/:pollingUnitId/timeline at apps/api/src/routes/evidence.ts:968, authorized by territoryForPollingUnit + canAccessTerritory (404 otherwise), merging AgentActivity (check-in/check-out/location pings), Incident, ElectionDayReport and EvidenceAsset into one chronologically s… | apps/api/src/evidence.test.ts asserts the timeline contains the INCIDENT id and the EVIDENCE id with its sha256. | Communications metadata is not in the timeline at all (VoiceCall/VoiceCallEvent/Message models exist at schema.prisma:1672/1727/1787 but are never queried here), and review actions / custody events are absent even though the spec lists them. Opening and counting are not distinct events (only Electi… | — |
| 133 | Polling Unit Evidence Dossier | PARTIAL | GET /evidence/polling-units/:pollingUnitId/dossier at apps/api/src/routes/evidence.ts:995 returns the PollingUnit record, resolved territory, a completeness block (evidenceCount, photoCount, videoCount, writtenReportCount, incidentCount, reportCount, custodyEventCount), all evidence serialized with… | apps/api/src/evidence.test.ts asserts evidenceCount 1, incidentCount 1, custodyEventCount >= 4, evidence entries carrying sha256, and legalConclusion === null. | Missing several items the spec enumerates: assigned coordinator (never resolved), GPS history (AgentActivity location pings are not included), communication metadata (VoiceCall/Message not queried), and audit records (AuditLog not queried). Review notes are only reachable inside EvidenceCustodyEven… | — |
| 134 | Ward and Constituency Evidence Aggregation | PARTIAL | GET /evidence/aggregation at apps/api/src/routes/evidence.ts:586 groups by POLLING_UNIT / WARD / STATE_CONSTITUENCY / FEDERAL_CONSTITUENCY / SENATORIAL_DISTRICT, applies evidenceWhereForActor scoping plus type/classification/status/date filters, and returns per-territory evidenceCount, latestServer… | apps/api/src/evidence.test.ts 'evidence explorer and ward aggregation expose scoped post-election discovery' asserts a WARD group for the test ward with evidenceCount >= 1. | Aggregation is computed in application memory over prisma.evidenceAsset.findMany({ take: 1000 }) ordered by serverReceivedAt desc (sourceLimit: 1000 is echoed in the response), so beyond 1000 assets every count is silently wrong — this is not a groupBy over the full set. There is no Ogun-State roll… | — |
| 135 | Evidence Search and Discovery | PARTIAL | GET /evidence at apps/api/src/routes/evidence.ts:517 with evidenceSearchSchema supporting free-text search (id, file name, sha256, incidentId, electionReportId, pollingUnitId), evidenceType, classification, reviewStatus, pollingUnitId, incidentId, electionReportId, uploaderUserId, sha256, dateFrom/… | apps/api/src/evidence.test.ts asserts search by id returns the asset with its sha256, that originalAccess.signedAccessRequired is true, and that summary counts are populated. | 'Territory' search is only Polling-Unit deep: there are no wardId / stateConstituencyId / federalConstituencyId / senatorialDistrictId filter parameters, so an authorized state officer cannot search 'all evidence in Ward X'. 'Reporter' search exists only as an exact uploaderUserId parameter with no… | — |
| 136 | Controlled Legal / Evidence Export | PARTIAL | POST /evidence/exports/manifest at apps/api/src/routes/evidence.ts:1135, reviewer-gated and re-scoped through evidenceWhereForActor, builds a manifest with per-item evidenceAssetId, type, classification, review status, sha256, storage key/bucket, file name, mime, size, serverReceivedAt, PU/incident… | apps/api/src/evidence.test.ts 'legal-support association and controlled manifest export are audited' asserts itemCount, manifestSha256 format, persisted package items, EXPORTED cu… | No package is ever materialized — the response hard-codes archivePackagingStatus 'TARGET_LATER_MANIFEST_ONLY'. The export contains no evidence files, no reports, and no supporting records, and there is no endpoint to download an assembled archive; the spec's 'evidence packages containing selected e… | — |
| 137 | Evidence Access Control | PARTIAL | apps/api/src/routes/evidence.ts enforces access server-side: canAccessTerritory() checks actor.isActive and accountStatus === 'ACTIVE', restricts to OGUN_STATE_ID, allows SUPER_ADMIN/STATE_OFFICER/VALIDATOR, checks agentProfile.pollingUnitId for AGENT, and runs resolveOperationalTerritory + authori… | apps/api/src/evidence.test.ts asserts a MEMBER receives 404 on POST /evidence/:id/access and that signed access returns publicUrl null with a memory:// signed URL. | No case/workspace permission dimension exists: LegalCase has no member/permission model, so every privileged reviewer sees and can modify every workspace state-wide. POST /evidence/legal-cases/:legalCaseId/evidence (evidence.ts:1099) loads the case with a bare findUnique and applies no legalCaseWhe… | — |
| 138 | Evidence Access Audit Trail | PARTIAL | apps/api/src/lib/audit.ts createAuditLog writes AuditLog rows (schema.prisma:1873) with actor, action, targetType/targetId and territory metadata. apps/api/src/routes/evidence.ts writes EVIDENCE_UPLOADED (upload), EVIDENCE_VIEW / EVIDENCE_DOWNLOAD (:842), EVIDENCE_REVIEWED including classification … | apps/api/src/evidence.test.ts asserts auditLog count for action EVIDENCE_DOWNLOAD == 1 and the matching DOWNLOADED custody event; export test asserts the EXPORTED custody event. | Two listed actions produce no AuditLog row: adding evidence to an existing workspace (POST /evidence/legal-cases/:id/evidence writes only a custody event) and manual custody-event creation (POST /:id/custody-events). The audit trail is also effectively unreadable for evidence: GET /admin/audit-logs… | — |
| 139 | Evidence Retention Policy | BLOCKED | Evidence is structurally protected from disappearing: EvidenceAsset.uploaderUserId and stateId are ON DELETE RESTRICT and CaseEvidence/EvidencePackageItem -> EvidenceAsset are RESTRICT (packages/database/prisma/ogun-migrations/20260809050000_evidence_closure_foundation/migration.sql lines 260, 272,… | No test exercises retention, legal hold, or any lifecycle behaviour. The only related assertion is that /evidence/storage/health returns the retention label string. | No approved retention or legal-hold governance policy exists. The repository contains only the hard-coded label ELECTION_EVIDENCE_STRONG_RETENTION_TARGET (apps/api/src/routes/evidence.ts:330,512) and a LEGAL_HOLD value on LegalCaseStatus. There is no retention period, expiry field, lifecycle model,… | GOVERNANCE |
| 140 | Post-Election Legal Support Workspace | PARTIAL | LegalCase, CaseEvidence, LegalCaseNote, EvidencePackage, EvidencePackageItem models with migrations (schema.prisma:2115-2210; ogun-migrations/20260809050000). apps/api/src/routes/evidence.ts:1038 creates a reviewer-gated, territory-validated workspace and associates evidence (re-scoped through evid… | apps/api/src/evidence.test.ts 'legal-support association and controlled manifest export are audited' asserts creation with noLegalConclusion true, evidenceCount 1 on listing, lega… | LegalCaseNote is a dead model — no endpoint or UI ever creates a note (grep finds it only in the test teardown), so 'review notes' in a workspace do not exist. LegalCaseStatus can never leave OPEN: no update/close/legal-hold route exists. Workspaces can only hold EvidenceAssets — reports, incidents… | — |

## Complete features (13)

018, 035, 037, 045, 047, 053, 054, 055, 109, 124, 125, 130, 131.

These share a profile: server-enforced, persistence-backed, and directly covered
by an integration test that exercises the actual workflow — territory
authorization, referral attribution and qualification gating, ledger
immutability and idempotency, payout batching/assignment/officer restrictions,
messaging permission rules, and the evidence custody/hash/classification/review
core.

## Blocked features (4)

| ID | Feature | Blocker type |
|---|---|---|
| 004 | Ogun Electoral Reference Database | EXTERNAL_DATA |
| 100 | Polling Unit Geofence Monitoring | EXTERNAL_DATA |
| 102 | Location Mismatch Alerts | EXTERNAL_DATA |
| 139 | Evidence Retention Policy | GOVERNANCE |

Nothing else is BLOCKED. Every other gap is ordinary unfinished engineering and
is classified PARTIAL, per the locked definitions.
